# HANDOFF — paste this into a new chat to resume Bexhearts

> **Purpose:** bootstrap a fresh AI session (or a new chat when the context window fills). This is a pointer + running notes, NOT the source of truth. Keep it updated as work progresses. Last updated: **2026-07-10**.

## 🆕 START HERE — fresh-chat brief (updated 2026-07-10 — this block is the authoritative resume point)

**Read `CLAUDE.md` → this block → `docs/PROGRESS.md`. Gate before "done": `npm run typecheck && npm run lint && npm test` (currently 187 passing). HARD RULES to re-read in CLAUDE.md: never run migrations (owner runs SQL in Studio); render-verify UI yourself via the booted sims (`xcrun simctl openurl <udid> "exp://127.0.0.1:8081/--/<route>"` + `simctl io <udid> screenshot` + READ it + scan device logs) BEFORE handing off; classify every change into `docs/PRIVACY_POLICY.md` / `docs/TERMS_OF_SERVICE.md`.**

### Environment facts
- Two iOS sims for couple testing (`npx expo start` → `shift+i` twice; test accounts Tony + Bexoni are a linked couple). Local Supabase: API 55321, Studio 55323, Mailpit (OTP inbox) 55324. `npx expo start -c` clears a stale bundle (many "bugs" were stale bundles).
- Edge functions: serve ALL of them — `supabase functions serve --env-file supabase/functions/.env` (avatar-upload-url + compose-prayer). `ANTHROPIC_API_KEY` (validated, `claude-haiku-4-5`) + R2 creds live in that env file, server-side only.
- **Migrations 00001–00021 ALL applied** (owner confirmed through 00021 on 2026-07-05). Owner should regen types when convenient: `supabase gen types typescript --local > src/types/database.ts` (database.ts was hand-updated through 00021 so it compiles).
- **Owner immediate step (may already be done):** bulk-approve + promote the 60 devotional drafts — `UPDATE public.devotional_drafts SET approved = true;` then `SELECT public.promote_approved_devotionals();` → Grow shows today's devotional; streak becomes earnable (anchor = BOTH partners complete today's devotional).

### 2026-07-10 session — decisions round (devotionals · pricing · marketing), all owner-locked
**Devotional repeat policy DECIDED:** communal rotation + grow the pool (per-couple journeys rejected); "you've read this before" affordance (surface the couple's prior reflection when a repeat lands) = v1.x. **`00023_devotional_local_day.sql`** — `get_today_devotional()` now uses the COUPLE's timezone for the day boundary (matches the 00012 streak day; no more ~4–5pm west-coast flip) AND fixes a latent 00020 bug (a `publish_date` override was skipped by the rotation OFFSET → RPC returned no rows on override days). **`00024_devotional_drafts_seed_2.sql`** — 40 new Sonnet-drafted devotionals (pool → 100; first repeat ~100 days out; fresh themes: distance, money, in-laws, tech/attention, friendship/laughter, waiting, serving, rest/celebration), statically validated. **⚠️ OWNER: apply 00023, then run 00024 → review the 40 drafts in Studio (theological review) → `UPDATE devotional_drafts SET approved=true` (or per-row) → `SELECT promote_approved_devotionals();`**. Generator now PERMANENT at `scripts/gen-devotionals.mjs` (edit BATCHES each run; ~30/month; **Advent/seasonal overrides go on the calendar for ~late October** as `publish_date` rows). 00022 stays in migrations on purpose — the folder doubles as the owner's ordered VPS run-list (owner decision; the empty file now contains the bulk-approve script that lived in `Untitled`). **PRICING LOCKED: $79.99/yr (highlighted, "Save 49% · $6.67/mo for both of you") + $12.99/mo, per couple, 3-day trial; weekly $6.99 held for post-launch Superwall tests** — `plans.ts` + test updated; F2 creates store products at these prices; ToS change-log noted. **MARKETING: 2-month slideshow run APPROVED** — 60 posts × 4 theme accounts (240 total), delivered in 2-week waves of 15/account, cross-posted TikTok/IG/FB (add 4:5 1080×1350 exports for IG feed), wave 1 = waitlist CTA, Claude-vs-Codex labels + tracker kept (fair-test rules in MARKETING §2.10); stale "don't advertise" list from the 2026-06-29 Codex handoff corrected in §2.2 (Journal/Moments, milestones, prayer sessions, streak hub, rated date library are now LIVE angles). TikTok acct #1 live: 3 posts, ~2.7k views/150 likes. **Launch timing reality (agent assessment): store-live in 1 week is at risk** (F2/F3 products+keys, Apple sign-in verify + token revocation, Phase 9 prod backend, hosted legal URLs, real app icon/splash, review time) → target **TestFlight next week, store ~2 weeks**; content plan already assumes it. **NEXT: wave-1 content production (60 posts)**, then F2/F3.

**BOUNDARIES ROUND (same day, owner approved — sim-verified, gate green at 192 tests):** **template library v1** — `src/features/boundaries/templates.ts` (client-side constants, NO migration): 18 stage-aware boundary templates (every category covered) + 6 grace-framed temptation templates w/ action plans, rendered as a horizontal "Start from a template" rail that prefills the form (filtered by couple `relationship_stage` + selected category chip). **Form simplification:** why = optional; **action-plan field removed for plain boundaries** (temptation-only machinery; boundary = statement + why); **category-aware title placeholders** (`getTitlePlaceholder`); form keyed by register so tab-switching resets. **Pre-launch boundaries scope = done with this; mutual-agreement handshake + days-honored gamification = post-launch queue.** Owner eyeball items: tap a template card → fields prefill + chip selects; switch to Temptation Plan tab → "Common struggles" rail + plan field present.

**TRANSPARENCY ROUND D1–D7 (owner locked ALL pre-launch, same day — built, gate green 205 tests, key screens sim-verified):** **⚠️ OWNER: apply `00025_events_sharing_and_stamps.sql` BEFORE testing check-in submit or boundary retire/resolve — the app now writes the new columns (share flags, deactivation stamps) and errors until applied.** What shipped: **D1** stale-points fix (the 65-vs-85 was a never-invalidated cache — ledger was always right; `logActivity` now invalidates all points keys). **D2** `points-history` modal from the Us hub ("See how you earned them →": week/all-time totals, per-row activity/partner/date/+pts; couple-private, leaderboard still totals-only). **D3** check-in form now actually collects the 3 notes + sharing model (gratitude ALWAYS revealed once both submit — shown in the comparison card; growth/prayer private w/ per-field share toggles; toggles appear only when text is entered). **D4** `couple_events` audit trail (streak trigger logs grace_used/streak_reset; app logs boundary_retired/plan_resolved; prayers stamp archived_at) — feeds future recaps. **D5** boundaries: Victories 🏆 (resolved plans) + Past covenants (retired) sections w/ "Bring back", attribution ("Retired by X · date" once 00025 applied), **author-only Resolve on temptation plans**, realtime channel. **D6** journal: completed dates → `date-view` modal (edit stars/note — edits refresh the library aggregate; "Save as a Moment" → memory w/ photo flow); answered prayers → `prayer-view` (read-only + cached AI prayer). **D7 owner-modified: NO weekly recap ever** — gather via couple_events only; recap cards = year-end + app anniversary + relationship anniversaries; anniversary PRESET chips (💜💍💒 + first date + first "I love you") on the special-day form put them on the timeline. **AI disclosure sheet removed** (Compose tap silently stamps the consent the edge fn requires; Privacy Policy is now the disclosure surface — attorney/store-review flag added in PRIVACY_POLICY.md). Owner eyeball: check-in reveal on both sims (gratitude appears after BOTH submit), Resolve hidden on partner's temptation plan, tap a journal date/prayer entry.

**2026-07-11 bugfix round (owner testing feedback; ✅ owner has applied 00023 + 00024 + 00025):** (1) **Realtime remount crash FIXED** — "cannot add postgres_changes callbacks after subscribe()": supabase-js reuses the existing channel instance per topic → `freshChannel()` in realtime.ts removes the stale one first; all realtime hooks now clean up via `supabase.removeChannel()`. (2) **ALL toasts were silent** — `ui.store.showToast` set state nothing rendered (the real host is react-native-toast-message in the root layout); now bridged via `Toast.show()`, so note-saved/answered/copied feedback finally shows. Memory-note send also dismisses keyboard + haptic + error toast; photo upload toasts success/error. Gate green (205 tests), boot sim-verified clean. **Owner still to run in Studio (promotes the 40 reviewed batch-2 drafts): `UPDATE public.devotional_drafts SET approved = true; SELECT public.promote_approved_devotionals();`** (expect 40 → pool 100). **Dark mode reminder: NOT built yet** — theme is light-only + `userInterfaceStyle: 'light'` forced; the Appearance selector only persists the preference. Owner has asked 3× → schedule the Phase 8 theme-refactor block as its own session NEXT.

**Same day — WEEKLY PLAN ADDED (owner reversed the 07-10 hold):** paywall now shows **$6.99/wk · $12.99/mo (≈$3/wk) · $79.99/yr (≈$1.54/wk, "Save 78%" badge, default)** — three ascending cards, sim-verified; `plans.ts`/`purchaseTrial` (now handles `offering.weekly`)/tests updated (205 green). **Trial mechanism confirmed for owner:** store-native subscription purchase w/ free-trial intro offer = payment method committed up front, $0 today, auto-billed at trial end (the only store-permitted model). **F2 setup guide delivered to owner** (App Store Connect subscription group `Bexhearts Premium` w/ 3 products each carrying a 3-day free-trial intro offer → RevenueCat project/entitlement `premium`/offering w/ $rc_weekly|$rc_monthly|$rc_annual → keys into `.env` → sandbox test **requires the dev build, NOT Expo Go**; Superwall = account + keys now, campaigns later since the paywall is owned). Per-couple entitlement alias + real enforcement = still the F1/F2 wiring TODO (Phase 5).

**2026-07-12 — F2 store setup DONE by owner + Expo Go guard added.** Owner completed: ASC agreements (bank CIBC, W-8BEN w/ treaty, GST/HST Form 506 — registered BN+RT per Apple's requirement), app record (name "Bexhearts" for now; ASO name/subtitle decision pending — options given, e.g. "Bexhearts: Faith for Couples"), subscription group `Bexhearts Premium` w/ 3 products (`bexhearts_premium_weekly|monthly|annual`), RevenueCat project (products, entitlement `premium`, offering `default` w/ $rc_weekly|$rc_monthly|$rc_annual packages — agent corrected an offering-misconfig where `$rc_weekly` was used as the OFFERING id), **real RC + Superwall keys now in `.env`**. Superwall = keys only, NO campaigns/SDK checklist at launch (owned paywall; its integration prompt deliberately ignored — revisit for post-launch A/B tests w/ the RevenueCat purchase-controller guide). **Code: `src/lib/runtime.ts` `isExpoGo` guard added to `isRevenueCatConfigured()`/`initRevenueCat`/`initSuperwall`** — with real keys, Expo Go must still behave unconfigured (dev-unlocked gating, paywall pass-through) or the daily loop breaks; dev build gets real purchases. Gate green (205). **Owner next: clear "Missing Metadata" on all 3 products (localization + price + 3-day free-trial intro offer + review screenshot), `npx pod-install`, rebuild dev build via Xcode, sandbox purchase test.** Then agent builds F2 enforcement (per-couple RC alias + real gating). Business decisions recorded: launch WITHOUT EU storefronts (DSA trader display deferred), seller name = personal (org account + "Bexoni Labs" needs incorporation later; app transfer path), Small Business Program enrollment (15%) once Paid Apps agreement is Active.

**Same day (2026-07-12, later) — WELCOME REDESIGN + LANDING SITE OVERHAUL:** (1) **Onboarding welcome rebuilt** (owner picked B+C+D from 5 agent mockups; Unfold app = the style reference): fanned polaroids (photos pending — owner has rights; drop 2 files + `require()` into `PHOTOS` in `app/(onboarding)/welcome.tsx`) + feature pills + **Ecclesiastes 4:12 as the serif hero** + "Earn your first 10 points together today" chip + square Get Started. Serif gotcha fixed: use `fonts.serif.bold` from the theme, never `fontFamily: 'serif'` (invalid on iOS). Sim-verified, gate green (205). (2) **Subscription display names LOCKED (owner):** "Bexhearts Premium – Weekly / – Monthly / – Annual", description "Everything, together one plan, two hearts" (owner removed the em dash) — owner types these into each product's ASC localization. **Review screenshot generated → `~/Downloads/bexhearts-subscription-review-screenshot.png` (1290×2796)** — upload to all 3 products. (3) **Landing site (separate repo `/Users/Ebuka/Projects/bexheartslandingcodex`, Next.js 15, deploys to the purchased `bexhearts.com`) overhauled:** domain/email → bexhearts.com/support@bexhearts.com (⚠️ owner must create that mailbox); self-referential "dark section" → real **per-couple pricing section** ($6.99/$12.99/$79.99, 3-day trial, partner-joins-free); FAQ expanded w/ pricing + feature answers; **FAQPage + SoftwareApplication JSON-LD w/ real offers**; **full Privacy + Terms pages ported from `docs/PRIVACY_POLICY.md`/`TERMS_OF_SERVICE.md`** (draft-for-attorney banner kept, internal changelogs stripped — keep the site pages in sync with the docs from now on, same-pass rule); **blog 4 → 12 posts** (8 new SEO articles adapted from the marketing angles, staggered dates); **llms.txt expanded** (product summary, pricing, privacy highlights, full guide list). `next build` clean. **App now points at the real URLs** (`src/constants/app.ts` → bexhearts.com; support email updated). **⚠️ THE WAITLIST PAGE NOW EXISTS → Wave 1 L2/L3 CTAs unblock the moment the owner deploys the site (Vercel + the waitlist Supabase env vars per its `.env.example`) and points DNS.** Sandbox purchase test = owner-parked to LAST (after everything else is ironed). **✅ owner created the support@bexhearts.com mailbox.** **WAVE 1 CONTENT DELIVERED (2026-07-12): `bexhearts_content_wave1_claude.xlsx`** in the handoff folder — 60 Claude Code posts (Days 11–25, 15/account, 9/4/2 CTA mix, waitlist wording, 347 slide-copy rows, tracker pre-filled); CONTENT_NOTES.md updated. Wave 2 due after ~2 weeks of tracker data.

### Where the build is (2026-07-05, everything gate-green + sim-verified)
Core loop D1–D7 ✅. Journal (Moments + Milestones — renamed from Memory/Special day) with multi-photo upload to R2, fanned polaroids, full-screen viewer, reactions w/ note send button ✅. Custom calendar w/ native month/year wheels ✅. Prayer stack: personal/shared prayers, edit, Ours|Mine filter, **prayer-focus session** (durations, keep-awake, Amen→activity) with **AI-composed prayers via `compose-prayer` edge fn** (Haiku, one-time consent stamped on profile, crisis guard, unsuitable-input guard, responds in request language, cached per prayer) ✅. **Streak A+D**: Home card (week dots, grace chip, trackables) → **Us hub** (heatmap, per-activity streaks, badges, points) → **global leaderboard** (opt-in names, masked `t**y & b****i · #1001` + country flag otherwise) ✅. **Points**: SECURITY-DEFINER trigger on `activity_log` (devotional 10 / prayer 15 / check-in 20 / journal 10 / date 25 / rating 5; daily-unique per user/type) ✅. **Date library**: 350 ideas, 7 categories, metadata (cost/location/accessibility/season/stage), completion stars also rate the idea → couple-level global aggregates ("★4.6 · N couples") ✅. **Devotional pipeline**: calendar-communal evergreen-pool rotation (`get_today_devotional()`; dated rows = seasonal overrides), `devotional_drafts` staging (app can't read), `promote_approved_devotionals()`, 60 Sonnet-drafted launch devotionals, generation script pattern in scratchpad `gen-devotionals.mjs` (~30/month cadence) ✅. Settings flattened onto Profile (appearance pref persisted — dark THEME itself not built), legal drafts live ✅. Auth hangs: root-caused twice + fixed (sync onAuthStateChange callback; bounded auth calls; couple context hydrates before setSession) ✅.

### NEXT — prioritized to-do
1. **Owner verify loop:** promote devotionals → both sims complete today's devotional → streak lights + points; compose an AI prayer (consent sheet → Haiku result); memory with photos (picker now presents full-screen); complete + rate a library date → aggregate appears.
2. **Dates v2 — suggest → accept flow:** partner suggests an idea → lands in Our Dates as Suggested w/ in-app realtime nudge (push at G1) → accept/pass. Rating pipeline + points already built.
3. **Notification system (G1+):** push infra, then categories — transactional (always on) / partner activity (praying-for-you, date suggested/accepted, reactions) / engagement (streak nudges, prayer digest, points). Update PRIVACY_POLICY per category when built.
4. **Dark mode** (Phase 8 theme refactor, ~50 files, dedicated block — owner has asked twice; appearance pref already persisted and must be honored).
5. **NEW (owner, 2026-07-05): long-distance couples mode** — design round first: timezone-aware "their time" display, virtual-date category in the library (watch-together, video-call dinner), LDR-aware date filters, possibly countdown-to-next-visit as a first-class Milestone. Iron out with owner before building.
6. **NEW (owner, 2026-07-05): breakup / unlink handling** — product + data design: an explicit "unlink" flow (today only account-deletion hand-off exists): what happens to shared journal/prayers/photos/points/streak on breakup (proposal: archive couple space read-only? export? split?), solo-mode afterlife, re-linking rules. MUST update ToS/Privacy in the same pass.
7. **Widgets (E7) — decision recap:** prior decision (2026-06-21) = iOS WidgetKit FIRST, Android later/maybe; needs dev build + config plugin (e.g. @bacons/apple-targets) + App Group; NOT Expo Go. **Owner peg (2026-07-05): widget v1 content = today's devotional (title + verse ref) + streak flame** — pairs perfectly with calendar-communal (one shared content/day). Schedule as a spike right after the next dev-build pass.
8. ~~**Devotional repeat policy — decide next session**~~ **✅ DECIDED (owner, 2026-07-10):** option (a) — communal rotation + grow the pool; "you've read this before" affordance (prior reflection resurfaces on a repeat) queued for v1.x. Pool now 100 after 00024. See the 2026-07-10 session block above.
9. **F2/F3 paywall** (store accounts + products + 3-day trial intro + RC/Superwall keys; Restore Purchases UI exists) · **onboarding quiz expansion** · **Bible reader in Grow** · **i18n/localization pass** (AI prayers already reply in-language; app strings are English-only) · **store-compliance pass** · rest of engagement layer (E1 daily question, E2 mood, E3 praying-for-you, E4 challenges).
10. **Points/rewards ops:** values tunable in the 00017 trigger; eligibility skeleton in ToS §7 (sub tenure, both-partners-active, anti-abuse, no-purchase-necessary route); owner meets lawyer later; leaderboard copy says "exciting rewards on the way" only.

### Files to peruse (by area)
- **Auth/boot:** `src/providers/AuthProvider.tsx` (sync callback + bounded boot + hydrate-before-setSession), `src/services/supabase/client.ts`, `app/index.tsx` (gate trusts profile.couple_id).
- **Prayer focus/AI:** `app/modal/prayer-focus.tsx`, `src/features/prayer/focus.ts`, `supabase/functions/compose-prayer/index.ts`, `src/api/prayers.ts` (useComposePrayer/consent).
- **Streak/points/leaderboard:** `src/features/dashboard/activity.ts`, `src/api/activity.ts`, `src/api/points.ts`, `app/modal/us-hub.tsx`, `app/modal/leaderboard.tsx`, migrations 00016/00017.
- **Date library/ratings:** `src/api/dates.ts` (aggregates + rate), `app/modal/date-complete.tsx`, migrations 00018/00019.
- **Devotional pipeline:** `src/api/devotionals.ts` (RPC hook), migrations 00020/00021, scratchpad `gen-devotionals.mjs` pattern (HANDOFF is the spec: batches via Anthropic API w/ owner key, Sonnet, themes list, validation).
- **Journal/photos:** `src/api/journal.ts`, `src/api/uploads.ts`, `src/lib/imagePicker.ts` (FULL_SCREEN fix), `app/(tabs)/journal/[id].tsx`.
- **Legal:** `docs/PRIVACY_POLICY.md` + `docs/TERMS_OF_SERVICE.md` (+ the sync rule in CLAUDE.md).

---

## 1. Read these first (in order)
1. **`CLAUDE.md`** (root) — agent protocol + the doc index + cross-update rule. (`AGENTS.md` is the identical mirror for Codex/Antigravity.)
2. **`docs/PROGRESS.md`** — WHAT & WHY + status (positioning, features, phases, decisions, known bugs). The source of truth.
3. **`docs/STEPS.md`** — HOW & in what ORDER. **Its "👉 Current position" marker tells you exactly where we are and what's next.**
4. **`docs/MARKETING.md`** — go-to-market (only when doing marketing).
5. **`README.md`** — how to run the app + local Supabase.

## 2. What Bexhearts is (one paragraph)
A subscription **mobile app (Expo/React Native + Supabase)** for **Christian couples** — daily devotionals, shared prayer journal, weekly check-ins, boundaries, date ideas, streaks, plus a "daily-touch" engagement layer (daily question, mood, "praying for you", challenges, milestones, memories). Positioning is **faith-forward**, beachhead audience **dating → engaged → newlywed**. Monetization: **free trial → hard paywall, billing per-COUPLE** (invited partner inherits, never a second paywall). Solo is a *state, not a segment*.

## 3. Where we are NOW (update this as we go)
- **🟢 LATEST (2026-07-05e) — DATE LIBRARY BUILT: 350 ideas + ratings pipeline. ⚠️ OWNER runs TWO migrations in Studio: `00018_date_library.sql`** (date_ideas metadata: location_type/accessibility_tags/season/stage_fit; `date_idea_ratings` table — review text couple-private via RLS; `get_date_idea_aggregates()` SECURITY DEFINER RPC returning couple-level averages) **then `00019_date_library_seed.sql`** (340 new ideas → 350 total across 7 categories incl. NEW `service` + `food`; statically validated: 340 unique titles, all enum/tag values constraint-clean). Client (gate green, 187 tests; dates screen sim-verified): categories constant +service/+food; `useDateIdeaAggregates` + `useRateDateIdea` (rates earn `date_rated` points); **date-complete stars now also rate the library idea** (with "your stars help other couples" hint); `DateIdeaCard` shows "★ 4.6 · N couples". Privacy policy updated (ratings aggregate anonymous; reviews couple-private). **DEVOTIONAL PIPELINE APPROVED + BUILT (owner confirmed all 4 decisions 2026-07-05): ⚠️ owner runs `00020_devotional_pipeline.sql` then `00021_devotional_drafts_seed.sql` in Studio.** Design: calendar-communal (everyone shares today's devotional) · evergreen pool rotation (`get_today_devotional()`: a `publish_date` row overrides, else pool rows with `sequence` rotate by days-since-2026-01-01 mod pool size — never runs dry) · AI-drafted/owner-approved (60 Sonnet-drafted launch drafts in `devotional_drafts`, RLS-invisible to the app; **owner reviews/edits each in Studio → sets `approved = true` → runs `SELECT public.promote_approved_devotionals();`**) · personalization deferred (stage_tags/focus_tags — matching onboarding `GROWTH_FOCUS_VALUES` — stored now, per-couple filtering at ~150+ pool). Client: `useTodayDevotional` → the RPC (empty pool → graceful "No devotional today"); `DevotionalCard` handles null publish_date ("Today"). Monthly cadence after launch: ~30 drafts/batch via the generation script (scratchpad `gen-devotionals.mjs` pattern; regenerate any time). (1) **Memory note UX:** explicit purple send button + "Note saved ❤️" toast + your own note prefills the field (was: silent save on keyboard blur — owner couldn't tell it saved). (2) **Streak-zero copy** now explains the anchor ("Both finish today's devotional to light the flame") — owner saw filled activity dots next to a 0 streak and read it as broken; the D6 rule (both partners complete the daily devotional) is unchanged. (3) **Dates chips descender clipping** on the All filter fixed (filtersScroll fixed height 44). (4) **✅ owner applied 00017** — points + masked leaderboard verified live (10 pts, `t**y & b****i · #1001` 🇨🇦). (5) **DATE-RATING AGGREGATION LOCKED (owner asked, agent design):** ratings are per-USER rows (`date_idea_ratings`: idea_id, couple_id, user_id, rating, review; unique idea+user) but aggregate at the COUPLE level — a couple contributes ONE rating (the average of whichever partners rated, 1 or 2); the global figure averages couples equally ("4.6 · 212 couples") so two-rater couples aren't double-weighted; a solo rating simply IS the couple's rating until the partner adds theirs. Rating earns `date_rated` points (5, already in the 00017 trigger). (6) **Date library APPROVED — generate 350 ideas** (schema incl. category/cost/location/accessibility/stage/season + the ratings tables) as its own dedicated seed-migration pass NEXT (owner note: engagement tracking + reviews + aggregate rating must ship with it). Gate green (**187 tests**). **⚠️ OWNER: apply `00017_points_and_leaderboard.sql`** (points_ledger written ONLY by a SECURITY DEFINER trigger on activity_log — client can't forge points; daily farming capped by activity_log's per-user/type/day uniqueness; values: devotional 10, prayer 15, check-in 20, journal 10, date 25, rating 5 — tune in the trigger; couples get `leaderboard_opt_in` (default FALSE), `leaderboard_number` (human-friendly sequence from 1000, NOT a uuid), `country_code`; `get_leaderboard()` RPC masks names server-side — "t**y & b****i · #1042" unless opted in). Client: `src/api/points.ts` (total, leaderboard, opt-in toggle + device-region country via **expo-localization**, new dep — Expo Go bundled), Us hub **points card** ("earned together — exciting rewards are on the way") → **`app/modal/leaderboard.tsx`** (opt-in switch, rank/flag/label/points, your row highlighted); `date_completed`/`date_rated` activity types wired (dates completion logs now). **LEGAL: `docs/PRIVACY_POLICY.md` + `docs/TERMS_OF_SERVICE.md` drafted** (attorney-review DRAFTS; cover auth incl. Apple/Google data, AI prayers w/ consent + no-training statement, photos, points/leaderboard pseudonymity, anticipated reward eligibility skeleton, bans + partner effect, 7-day deletion) + **standing rule in CLAUDE.md/AGENTS.md: classify every future change into these docs in the same pass.** Rewards eligibility guidance recorded in ToS §7 (sub tenure, both-partners-active, anti-abuse, no-purchase-necessary route, ID verification at claim). **Date-idea library: schema + sample given to owner for approval BEFORE the 350-idea generation** (fields: category, cost tier, location type, accessibility notes, stage fit, season, indoor/outdoor). Gate green (**187 tests**).
- **🟢 (2026-07-05b) — owner-feedback round (sim-verified).** (1) **Prayer edit** — author-only Edit action on `PrayerItem` → `prayer-form` edit mode (title/body/visibility); answered action relabeled "Answered 🙌". (2) **AI guardrails hardened** — `compose-prayer` prompt now returns `{"unsuitable": true}` for lorem-ipsum/gibberish/vulgar/off-mission requests (client shows a gentle "reword it" card, nothing cached) and **responds in the request's language** (verse translated, ref kept). App-level i18n = separate later effort (store listings don't auto-translate). (3) **Photo-picker stuck AGAIN root-caused:** the picker sheet presented OVER our modal swallows its own confirm/cancel taps on iOS → `presentationStyle: FULL_SCREEN` in `src/lib/imagePicker.ts` (owner re-test). (4) **Dates category chips blob FIXED** (horizontal ScrollView lacked `flexGrow: 0` → chips stretched vertically; verified). (5) **Settings FLATTENED onto Profile** (owner ask): `SettingsSections` component rendered directly on the profile tab; `profile/settings.tsx` deleted. (6) **RENAMES (de-CoupleJoy):** UI strings Memory → **"Moment"**, Special day → **"Milestone"** everywhere (tables/routes unchanged). (7) **Dark mode** still expectation-gapped for the owner — agreed path: dedicated Phase 8 theme-refactor block next time it's prioritized. **PENDING OWNER DECISIONS (dates v2 + points — designed, not built):** partner suggests date → other likes/accepts (needs in-app realtime nudge now, push at G1); bigger date-ideas library (J2 content); date completion → prompt to save as a Moment w/ photos + optional Milestone; **global date-idea ratings** (couples rate after completing; shown to other couples); **points ledger** (earned from tracked activities incl. prayers/dates/ratings, shown in Us hub as "working towards the prize draw", quarterly/biannual couple-getaway promotion funded by owner) — points = a new `points_ledger` table + rules TBD with owner before build. Gate green (**185 tests**).
- **🟢 (2026-07-05) — PRAYER FOCUS + AI (Haiku) + STREAK A+D BUILT (all screens sim-verified, zero log errors).** **⚠️ OWNER: (1) apply `00016_prayer_focus_and_activity.sql`** (activity_log table + prayers ai_* cache columns + profiles.ai_prayer_consent_at — activity/AI features no-op gracefully until applied); **(2) restart the functions server to serve ALL functions:** `supabase functions serve --env-file supabase/functions/.env` (the old command served only avatar-upload-url; compose-prayer needs serving too); (3) regen types when convenient. **Anthropic key validated** (direct API call → `claude-haiku-4-5` responds; key in `supabase/functions/.env`, server-side only). **Built:** (a) **`compose-prayer` edge fn** — JWT auth → RLS-scoped prayer load → consent enforced server-side → crisis-pattern guard (returns `flagged`, no AI call) → Haiku composes 60-110-word prayer + ONE WEB verse (JSON) → **cached on the prayers row** (one generation per prayer) ; (b) **prayer focus session** `app/modal/prayer-focus.tsx` — duration chips 3/5/10, keep-awake, one prayer per screen (title + verse + AI prayer), "Compose a prayer" w/ **one-time consent sheet** (stamped on profile), crisis prayers get help-resources copy instead, Amen → logs `prayer_session` activity; (c) **hybrid IA** — prayers screen renamed "Prayers" + **Ours | Mine** scope chips + **Pray now** button; Grow gets a "Prayer time" entry card; (d) **streak A+D** — `activity_log` written best-effort by devotional/check-in/journal/prayer-session mutations (`src/api/activity.ts`, idempotent per user/type/day); **StreakCounter upgraded** (week dots, grace-day chip, trackable chips, tap → Us hub); **`app/modal/us-hub.tsx`** (hero streak + names/days-together, 12-week GitHub-style heatmap, per-activity streaks, badges). Pure logic + tests: `src/features/dashboard/activity.ts`, `src/features/prayer/focus.ts` (`__tests__/features/activity.test.ts`, `prayerFocus.test.ts`). Gate green (**185 tests**). **Owner live-verify after 00016 + serve:** create a shared + a personal prayer, run Pray now (compose consent → AI prayer + verse appears; partner sim shows the shared prayer's cached AI prayer), complete a devotional → Home dots/chips fill, open Us hub → heatmap + streaks move. (1) **"Keeps asking me to invite my partner" ROOT-CAUSED + FIXED:** the route gate (`app/index.tsx`) trusted the async couple STORE (`coupleId`) which hydrates after boot → every cold start redirected LINKED users to partner-invite. Fix: gate now trusts **`profile.couple_id`** (the row it already fetched) with the store as fallback, AND `AuthProvider` boot now hydrates couple context (bounded 5s) BEFORE flipping `setSession` so the dashboard never renders unlinked. Verified: `simctl terminate` + relaunch → lands on the dashboard, both avatars, no invite card. (2) **GO_BACK-not-handled** (seen via deep links; possible from notifications later): `ModalHeader` close now guards `router.canGoBack()` → falls back to `router.replace('/')`. (3) **Calendar month/year = real scroll WHEELS** (owner ask, Couple Joy pattern): added **`@react-native-picker/picker`** (bundled in Expo Go — no rebuild now; the dev build needs `pod install` when we return to it) — tapping "July 2026" swaps the grid for native month + year wheels (years **1910 → current+15**) with a Done link. Wheel needs a tap to open, so owner eyeballs that bit. (4) **✅ owner applied `00015`** (prayer visibility) — prayer create/personal-shared is live-testable. Gate green (**171 tests**).
- **🟢 (2026-07-04g) — bugfix + prayer-visibility round (all render-verified on the sims; new HARD RULE in CLAUDE.md/AGENTS.md: render-verify UI + scan device logs yourself before handing off).** (1) The `isValidDate` red-screen the owner hit was a **mid-edit stale bundle** (current code has no such reference; verified clean + no JS errors in device logs after reload). It also explains the "can't close the photo picker" report — the app was red-screened UNDERNEATH the native picker; retest photos now. (2) **Emoji icons clipped at the top** (Home quick actions, Connect cards — the "sheared heart"): emoji overflow the latin font box when `fontSize` has no `lineHeight` → added lineHeights (`QuickActions.tsx`, `connect/index.tsx`); before/after screenshots confirm. (3) **Calendar UX:** now **closed by default** (opens only from the "Pick a date" field) and the **month title toggles a month grid + year-stepper picker** (Couple Joy pattern, JS-only). (4) **Settings:** `mailto:` crash on sims (no Mail app → unhandled rejection) fixed with try/catch + copy-address-to-clipboard toast; all `Linking.openURL` calls now fail soft; appearance chips show a **persistent caption** ("dark theme ships in an upcoming update — saved") so the selector visibly responds. (5) **Prayers: personal vs shared BUILT** — migration **`00015_prayer_visibility.sql`** (**⚠️ owner MUST apply BEFORE creating prayers — the insert now sends `is_private` and errors until the column exists**): `prayers.is_private` + RLS (partner never sees personal prayers; personal managed by author only); `PrayerForm` gets a **Shared / Personal** selector (sim-verified), `PrayerItem` shows a "Personal" badge; `database.ts` hand-updated. NOTE: the owner's earlier "created a prayer, can't see it in Grow" = prayers live in **Connect → Prayers** (Grow is devotionals); their prayer "Heal me" IS in the DB. (6) **Prayer focus + AI (owner ask) — designed, next build:** in-app distraction-free session (timer + active shared/personal prayers + gentle guidance) entered from Connect→Prayers; **AI prayer generation** (analyze request → tie to a Bible verse → generated prayer) via a **server-side edge-function proxy** (DeepSeek or Haiku — key stays server-side; prayers are sensitive religious data → needs consent copy + privacy disclosure + a theological-safety pass). Gate green (**171 tests**).
- **🟢 (2026-07-04f) — UX round (owner asks): custom calendar, modal close buttons, Settings expansion; + two verified fixes.** (1) **"Create a memory did nothing" FIXED:** `journal-add` chooser did `router.back()+push()` — the push raced the dismiss animation and was dropped (zero rows ever written); now `router.replace()`. Backend proven healthy end-to-end via Node repro (insert→presign→R2 PUT→public URL all green). (2) **Active-tab pill clipping FIXED:** bottom-tabs' ~25px icon slot clipped the 34px pill to a sliver → explicit `tabBarStyle.height: 62+insets.bottom` + `tabBarIconStyle: 56×34`; **verified via `xcrun simctl` screenshots** (this session drives the booted sims directly: `simctl io <udid> screenshot` + `simctl openurl <udid> "exp://127.0.0.1:8081/--/<route>"` deep links — use this to render-verify UI). (3) **Custom in-app calendar** (owner ask; Couple Joy pattern, JS-only, no rebuild): `src/lib/calendar.ts` (pure month-matrix, tested) + `src/components/ui/Calendar.tsx` (month header + chevrons, SUN–SAT grid, purple selected disc, tinted today) — replaces the typed `YYYY-MM-DD` input in special-day-form (pressable date field → inline calendar; sim-verified). (4) **`ModalHeader` close affordance on ALL 8 `app/modal/*` screens** (X → `router.back()`; title where the screen had one) — no full-screen modal is a dead end anymore. (5) **Settings expanded** (`profile/settings.tsx`): **Appearance System/Light/Dark** chips persisted via `ui.store` + AsyncStorage (`bexhearts.appearance`; dark theme itself is Phase 8 — picking Dark toasts "arrives in an upcoming update", pref applies the moment theming ships), **Restore Purchases** (graceful when RC unconfigured — closes the F2 to-do item), Share the app (native share sheet), Rate (store link constant, honest toast pre-launch), About us, support/legal links, app-version footer, BackButton. New constants `WEBSITE_URL`/`APP_STORE_URL`/`SHARE_MESSAGE` in `src/constants/app.ts`. Gate green (**171 tests**). Calendar reuse candidates later: date scheduling (`SchedulePicker` presets could offer "pick a date"), memory backdating.
- **🟢 (2026-07-04d) — sign-in hang root-caused AGAIN + fixed; photos/gallery BUILT; Journal polish done.** (1) **Sign-in spinner hang:** proven by Node repro — supabase-js awaits the `onAuthStateChange` callback while `signInWithPassword` is pending; our async callback (hydration + SDK init) blocked it. Callback now **synchronous** (hydration deferred `setTimeout 0`), `authService.onAuthStateChange` typed sync-only, signIn/signUp/verifyOtp bounded by 12s `withTimeout` (`src/utils/withTimeout.ts`); tests `__tests__/providers/authProvider.test.tsx` + `__tests__/utils/withTimeout.test.ts`. (2) **Photos/gallery (task #1) BUILT:** edge fn generalized (kind `avatar`|`memory`, one bucket two prefixes, memory membership proven via RLS read); shared `src/api/uploads.ts` (presign→PUT→publicUrl, avatars refactored onto it); `src/lib/imagePicker.ts` multi-select; `useAddMemoryImages` (upload + ordered `memory_images` rows via pure `buildMemoryImageRows`); memory-form picks/removes photos and uploads on save; detail screen has add-photos + polaroids→**PhotoViewer** (full-screen, paging, counter, share, iOS pinch-zoom via ScrollView zoom; cream letterbox); timeline select now embeds `memory_images` → polaroids render on feed cards. (3) **Journal polish (task #6) DONE:** special-day-form has **edit mode** (tap a milestone card → prefilled params) + **Remove** with confirm (`useUpdateMilestone`/`useDeleteMilestone`); `subscribeToJournal` now also watches `memory_images` + `memory_reactions` and invalidates the open memory by `memory_id` (reaction realtime). **⚠️ Owner: apply `00014_memory_images_realtime.sql`** + serve the edge fn when testing uploads. **Gate green: 160 tests.** Remaining gallery niceties: Android pinch-zoom, image delete/reorder, memory edit/delete.
- **🟢 (2026-07-04c) — testing fixes + product decisions.** (1) **Partner-link "Something went wrong" bug:** the `link_partner` RPC raises "You are already linked to a partner" but `getErrorMessage` didn't map it → generic error. Mapped it now; the real cause during two-sim testing is the **joiner account already has a couple from a prior run** → to link cleanly, use a FRESH second account (or clear that account's `couple_id`/couple in Studio). (2) **Deno editor errors** on `supabase/functions/*` (Cannot find module / Deno) are false — the file is Deno, checked by the Node TS server; excluded from `npm run typecheck`. Added `.vscode/settings.json` (`deno.enablePaths`) + `.vscode/extensions.json` + `supabase/functions/deno.json` → **install the recommended "Deno" VS Code extension** and they vanish. (3) **Tab active pill** was a lozenge — changed to a rounded-square chip (borderRadius 14) matching the reference; still a filled icon + label. (4) **Denomination** in profile-setup is now a **selectable chip list** (Non-denominational/Baptist/Catholic/…/Other), not a text field. Gate green (**144 tests**). **PRODUCT DECISIONS RECORDED:** in-app **Bible reader** in Grow (free/public-domain versions now, licensed NIV/ESV as premium later — task #32); onboarding questions currently only personalize *copy*, real content-tailoring waits on J2 content + the quiz expansion (PROGRESS Phase 4B note).
- **🟢 (2026-07-04b) — F1 paywall BUILT + auth-hang fix + R2 folder decision.** **Monetization LOCKED:** hard paywall, whole-feature gating, in-onboarding (after plan-summary, before partner-invite), framed as a **3-day free trial** ("Subscribe now — no charge today"); inviter starts, partner inherits. **Built the owned Option-B "trial timeline" paywall** — `src/features/subscription/components/Paywall.tsx` (Today/Day 2/Day 3 timeline + annual/monthly cards + "No charge today · then <price> · cancel anytime" + "billed per couple" + Restore + Terms/Privacy) at `app/(onboarding)/paywall.tsx`; `PlanSummary` now routes here (replaced the Superwall no-op). Purchase/restore → RevenueCat (`purchaseTrial`/`restorePurchases`); **entitlement defaults UNLOCKED in dev** (`checkPremium` → `__DEV__` when RC unconfigured) so testing + `PremiumGate` features work. Prices placeholder (`plans.ts`). Tested; gate green (**144 tests**). **F2/F3 remaining:** real products + 3-day trial intro offer in App Store Connect/Play + RevenueCat/Superwall config + keys → sandbox purchase → real enforcement + Profile "Restore". **AUTH-HANG FIX:** `getSession()` rejects on an invalid/expired refresh token (e.g. after a DB reset) → `AuthProvider` had no catch → stuck on "Loading…"; now wrapped (any failure → signed-out → sign-in). **R2 STORAGE DECISION:** owner added R2 env vars; use **ONE bucket with prefixes** `avatars/…` + `memories/…` (not separate buckets). Photo/gallery build (generalize the `avatar-upload-url` edge fn to both prefixes + memory multi-image upload + viewer) is now UNBLOCKED — next up alongside/after F1. **Dev tips locked:** `npx expo start -c` (clear cache), `supabase gen types typescript --local > src/types/database.ts` (regen), `shift+i` to run two iOS sims for couple testing (Mailpit OTPs at :55324). Owner regenerated `database.ts` (now the real generated file — stricter nullable types; fixed the fallout).
- **🟢 (2026-07-04) — IA restructure + Journal (E5+E6) BUILT.** Tabs are now **Home · Grow · Connect · Journal · Profile** (`app/(tabs)/_layout.tsx`): Devotional relabeled **Grow** (leaf icon, route dir still `devotional`), **Journal** added, **Dates** hidden via `href:null` (still reachable from Home quick actions), and the **active tab is a filled icon on a light-purple pill**. **Journal** (`app/(tabs)/journal/`): reverse-chron feed via `useTimeline` → pure `buildTimeline` (memories + milestones + answered prayers + completed dates; completed devotionals excluded to avoid daily flooding), B+C hybrid `TimelineEntryCard` (purple-family glyph rail + tinted milestone cards with `getCountdown` + fuller memory cards), month section headers, `useJournalRealtime` live sync. Create: `journal-add` chooser → `memory-form` (title/desc; photos deferred) + `special-day-form` (icon/title/date/color + countdown). Memory detail (`journal/[id]`) = **light reactions** (❤️/🙏 + one note per partner) + `FannedPolaroids` (renders when images exist). Hooks: `src/api/journal.ts`; new realtime channel `subscribeToJournal`. Migration **`00013_journal.sql`** (couple_milestones + memories + memory_images + memory_reactions + RLS + realtime pub). **⚠️ OWNER: apply `00013` + `supabase gen types typescript --local > src/types/database.ts`** (`database.ts` hand-updated so it compiles now). Tested `__tests__/features/journal.test.ts`; gate green (**141 tests**, typecheck, lint). **Photos + full-screen swipeable viewer = the gallery FAST-FOLLOW on a separate `bexhearts-memories` R2 bucket — blocked on the owner finishing the C3b R2 secrets** (which also unblocks avatar photo upload). **⚠️ Owner live-DB verify:** apply `00013`, add a Memory + a Special Day, confirm they + answered prayers/completed dates weave into the Journal feed and the partner sees them; react to a memory. **NEXT (owner-chosen order): finish C3b R2 secrets → F1 paywall (gate the now-built features) → onboarding quiz → store-compliance pass.** Remaining Journal polish: multi-image upload/viewer (R2), milestone edit/delete, reaction realtime.
- **🟢 (2026-06-29h) — DESIGN ITERATION (no code):** ironed out the **Journal / memories timeline (E6)** + related IA and onboarding with the owner. **LOCKED decisions** (full detail in PROGRESS Phase 4B): timeline = **B+C hybrid** (spine + typed icons for auto-entries, scrapbook hero cards for photo/date memories); **keep our purple/cream + square-button identity** (the v0 maroon/serif exploration is retired — no rebrand); **entry-type icons use one purple family** (glyph, not hue); **photo preview = fanned polaroids** → full-screen **swipeable viewer** (counter, pinch-zoom, share, close; shared-element spring open, cream letterbox, double-tap ❤️); **interaction = light reactions + one note** (not a comment thread); **active tab = filled icon on a light-purple pill**. **IA RESTRUCTURE LOCKED:** 5 tabs → **Home · Grow(devotional+challenges) · Connect(prayers+daily question+check-ins+boundaries) · Journal(memories+milestones+completed dates) · Profile** — this **relocates the D5 Dates tab** (ideas/planning → Home/Grow, completed dates → Journal). Manual Journal entries = **Memory + Special Day**, so **E5 milestones + E6 memories build together**. **Multi-image gallery** = a separate **`bexhearts-memories` R2 bucket** (distinct from avatars) + multi-select + slideshow — fast-follow on the C3b R2 pipeline; text/milestone memories + reactions build first. **Daily question (E1) → Connect**, not blended into boundaries. **ONBOARDING DECISION:** expand C1/C1b into a **4–6-Q couple quiz + richer plan reveal** (Noom/Duolingo pattern; inviter only, partner B inherits + skips paywall). **Still to iron out:** F1 paywall gating + the store-guidelines compliance pass. **Suggested next build:** IA restructure + E5/E6 Journal, then the onboarding quiz, then F1. Tasks queued (#27 gallery, #28 Journal, #29 IA, #30 onboarding). **No migrations pending from this round** (design only).
- **🟢 (2026-06-29g):** **D7 (remaining realtime) code DONE → ✅ CORE LOOP (D1–D7) COMPLETE.** `useCoupleRealtime()` (`src/api/couples.ts`) wires the last two channels — `subscribeToDevotionalProgress` + `subscribeToCoupleUpdates` — mounted once in `app/(tabs)/_layout.tsx`, so a partner's devotional completion (reflection reveal + streak bump) and any couple-row change reflect **live** on the other device (invalidate + cleanup on sign-out; tested `__tests__/api/coupleRealtime.test.ts`). Gate green (**135 tests**, typecheck, lint). **⚠️ Owner D7 live-DB verify:** two devices — partner completes today's devotional → the other sees the reveal + streak update without refreshing. **✅ All owner migrations 00008–00012 applied.** **NEXT — three iron-out rounds the owner requested before/around Stage E:** (1) **E6 memories-timeline UI design** (sources = answered prayers + completed devotionals + completed dates/photos; card types, chronology, empty state, R2-photo fast-follow); (2) **F1 paywall gating model + integration** (decide the free/premium content-volume split across every feature, per-couple entitlement, wire `PremiumGate` + Superwall); (3) **store-guidelines compliance pass** (LATEST Apple App Store Review + Google Play policies → first-try approval checklist: account deletion ✓, Apple sign-in + token revocation, subscription disclosure/restore/terms+privacy links, data-safety incl. religious-belief sensitive data, permissions, IAP rules). **⚠️ Simulator note (owner, 2026-06-29): seeing possibly-cached UI — clear with `npx expo start -c` (Metro cache) and/or rebuild if native changed.**
- **🟢 (2026-06-29f):** **D6 (streak engine) code DONE — fixes Bug #4.** `couples.streak_count`/`streak_last_date` existed + displayed but nothing wrote them; now a server-side trigger does. Migration **`00012_streak_engine.sql`**: `update_couple_streak()` (AFTER INSERT/UPDATE OF completed_at on `devotional_progress`) + new `couples.timezone` / `grace_days_remaining` / `grace_week`. **Rules LOCKED (owner, 2026-06-29):** anchor = **completing the daily devotional**; **everyone in the couple** must complete it that day (1 solo / 2 linked → a solo streak "levels up" into the couple streak on linking, same row); yesterday→+1, today→noop, a single missed day is covered by a **weekly grace day** ("grace covers you") else reset to 1; "today" is computed in the **couple's timezone** (avoids UTC off-by-one). Canonical logic is mirrored + thoroughly tested in `src/features/dashboard/streak.ts` (`computeStreakTransition` + `getStreakDayState`; `__tests__/features/streak.test.ts`) since the trigger itself is owner-applied. Dashboard now reads the **live** streak (`useMyCouple`, invalidated after devotional completion) and shows the **"waiting on your partner" nudge** — the core re-engagement mechanic. `createCouple` seeds `timezone` from the device (`getDeviceTimeZone` in `src/lib/dates.ts`). **⚠️ OWNER: apply `00012` in Studio + `supabase gen types typescript --local > src/types/database.ts`** (`database.ts` hand-updated so it compiles now). Gate green (**130 tests**, typecheck, lint). **⚠️ Owner D6 live-DB verify:** both partners complete today's devotional → streak increments; only one completes → stays + "waiting" nudge; simulate a missed day → grace holds it once per week, a 2-day gap resets. The boundaries "days-honored" streak (D4) will later ride on this engine. **NEXT: D7 — wire the last two realtime channels (`subscribeToDevotionalProgress` + `subscribeToCoupleUpdates`) so partner completions / streak changes reflect live; then the ✅ core-loop milestone.**
- **🟢 (2026-06-29e):** **D5 (dates) code DONE — "combine all 3, nothing forced".** The couple-dates loop was **broken** (you could save a date but no screen ever showed/completed it). Now: an **Ideas | Our Dates** segmented view on the dates tab; **Our Dates** groups by **derived status** Planned / Saved / **Memories** (`src/features/dates/dateHelpers.ts` — `getDateStatus`/`getDateTitle`/`partitionCoupleDates`, no status column) with mark-done + remove; **custom / log-your-own dates** (`useCreateCustomDate`; `app/modal/date-form.tsx`); **scheduling** via **JS-only quick presets** (`SchedulePicker`/`getSchedulePresets` — "This weekend/Next Friday…", deliberately NO native date-picker so no rebuild); **memory-first completion** (`app/modal/date-complete.tsx` — leads with the note, rating secondary); **discussion questions** (seeded but never rendered) now shown on the idea detail. Migration **`00011_couple_dates_custom_and_schedule.sql`**: `date_idea_id` nullable + `custom_title`/`custom_description`/`scheduled_for` + a source CHECK. **⚠️ OWNER: apply `00011` in Studio + `supabase gen types typescript --local > src/types/database.ts`** (`database.ts` hand-updated so it compiles now). Tested `__tests__/features/dateHelpers.test.ts`. Gate green (**121 tests**, typecheck, lint). **⚠️ Owner D5 live-DB verify:** save a library date, create a custom one, schedule one (preset), complete one with rating+note; partner sees them. **Dates decisions LOCKED (2026-06-29):** all-3 (loop+custom+scheduling); **memory-first *capture* now, unified "our story" *timeline* deferred to E6**; stage-aware date content deferred to J2 (category filter suffices); **date photos are a FAST-FOLLOW — they reuse the avatar R2 + `expo-image-picker` pipeline (C3b), which needs the owner's R2 secrets + a rebuild**, so completion captures rating+note now, photo later (there's a "photos coming soon" hint in the complete modal). **NEXT: D6 streak engine (Bug #4 — needs a migration for the increment/reset trigger; the boundaries "days-honored" streak later rides on this), then D7 remaining realtime.**
- **🟢 (2026-06-29d):** **D4 (boundaries) code DONE — "verify + polish only".** The boundaries screen now splits into two registers via `partitionBoundaries`: **Boundaries** (proactive shared covenants) vs **Temptation plans** (grace-framed copy — "with your partner beside you — no scores kept"). Added a **category** chip picker with a distinct taxonomy per type (`src/features/boundaries/categories.ts`) + category badge on cards; a **deactivate** action (Retire/Resolve → `is_active=false`); **either partner** can update/deactivate. Migration **`00010_boundaries_category_and_couple_update.sql`** adds `boundaries.category` (nullable freeform TEXT) + broadens the boundaries UPDATE policy to the couple. **⚠️ OWNER: apply `00010` in Studio + `supabase gen types typescript --local > src/types/database.ts`** (adds a column — `database.ts` was hand-updated to match so the app compiles now, but regen keeps it authoritative). Tested `__tests__/features/boundaries.test.ts`. Gate green (**112 tests**, typecheck, lint). **⚠️ Owner D4 live-DB verify:** create a boundary + a temptation plan with a category; partner sees them; either partner can deactivate. **Boundaries decisions LOCKED (2026-06-29):** keep the boundary/temptation split (different registers → different mechanics); **gamification = boundaries get a positive "days-honored" streak wired AFTER the D6 streak engine, temptations get NO relapse-breaking streak (a never-resets "victories" count instead — shame-avoidance)**; `category` added now to future-proof the curated template library + UGC content packs. **Marketing angle:** boundaries = the UGC/acquisition engine (each template = a shareable Reel), temptations = the depth/retention/testimonial engine (never an ad unit). **Deferred to their own steps:** stage-aware template library (content table + seed), mutual-agreement handshake (propose→accept), "I'm struggling now" accountability tap (needs push, E3/Phase 6). **✅ Owner applied `00009` (prayers couple-UPDATE) 2026-06-29.** **NEXT: D5 dates, D6 streak/Bug #4, D7 remaining realtime.**
- **🟢 (2026-06-29c):** **D2 (prayers + realtime) code DONE.** **Realtime:** `usePrayersRealtime()` (`src/api/prayers.ts`) subscribes via `subscribeToPrayers` and invalidates the prayers query on any partner add/answer/archive, tearing the channel down on unmount — called from `app/(tabs)/connect/prayers.tsx` (tested `__tests__/api/prayers.test.ts`). **Lifecycle UI:** the prayers screen is now a `SectionList` split **Praying / Answered** (`partitionPrayers`, tested), answered prayers move out of the active list; added an **Archive** action to `PrayerItem` + an **answered celebration** (success haptic + toast via `useUIStore`). **Migration `00009_prayers_couple_update.sql`** broadens the prayers UPDATE policy to the whole couple so **either partner** can mark answered/archive. **⚠️ OWNER: apply `00009` in Studio** (no type regen). Gate green (**107 tests**, typecheck, lint). **⚠️ Owner D2 live-DB verify:** two devices — a prayer added on A appears live on B; either partner can answer/archive. **Prayer-flow product decisions LOCKED (2026-06-29):** lifecycle = Active→Answered→Archived (no time-expiry); **either partner** controls lifecycle, author-only for text-edit + hard-delete; staleness = a later gentle "Still praying for this?" triage nudge (not expiry); reminders = **one daily prayer digest** in the daily touch (Phase 6), never per-prayer pings. Deferred to v1.x: praise-report/update thread on a prayer, answered→Memories (E6). **✅ Owner applied `00008` (check-ins UPDATE policy) 2026-06-29.** **NEXT: D4 boundaries, D5 dates, D6 streak/Bug #4, D7 remaining realtime.**
- **🟢 (2026-06-29b):** **D3 (check-ins) code DONE — fixes Bug #1.** **D3·M1:** new migration **`00008_check_ins_update_policy.sql`** adds the missing UPDATE RLS policy on `check_ins` (`USING`/`WITH CHECK user_id = auth.uid() AND couple_id = get_my_couple_id()`). **⚠️ OWNER: apply `00008` in Studio** — this is the one migration you don't have yet (no type regen needed). **D3·M2:** `useSubmitCheckIn` (`src/api/check-ins.ts`) now upserts with `{ onConflict: 'couple_id,user_id,week_of' }` so resubmitting the same week updates instead of throwing (tested `__tests__/api/check-ins.test.ts`). **D3·M3:** partner comparison — `src/features/check-in/comparison.ts` state machine (`locked`/`await-self`/`await-partner`/`revealed`) → new `CheckInComparison` card on the **Connect hub** (`app/(tabs)/connect/index.tsx`), gated by `couple.store.isLinked`, reveals both partners' ratings only once BOTH submit; new `useThisWeekComparison` hook (tested `__tests__/features/checkInComparison.test.ts`). Gate green (**101 tests**, typecheck, lint). **⚠️ Owner D3 live-DB verify:** after applying `00008`, submit a check-in then resubmit the same week (should update, not error); with two linked users, confirm the comparison reveals only after both submit. **NEXT: D2 (prayers + realtime), then D4 boundaries, D5 dates, D6 streak/Bug #4, D7 realtime.**
- **🟢 (2026-06-29a):** **Stage D STARTED — D1 (devotionals) code DONE.** **D1·M1 fixed Bug #2** — `useCompleteDevotional` (`src/api/devotionals.ts`) now upserts with `{ onConflict: 'devotional_id,user_id' }`, so re-completing a devotional updates instead of throwing a unique-constraint violation (tested in `__tests__/api/devotionals.test.ts`). **D1·M3 partner-reflection reveal** — each partner reflects privately; you see your partner's reflection only once BOTH have reflected. Pure state machine `src/features/devotional/reflectionReveal.ts` (`locked` / `await-self` / `await-partner` / `revealed`) → new `PartnerReflection` component rendered on `app/(tabs)/devotional/index.tsx`, gated by `couple.store.isLinked` so solo couples get a locked-until-partner state (not a paywall). Tested in `__tests__/features/devotionalReveal.test.ts`. Gate green (94 tests, typecheck, lint). **⚠️ Owner D1·M2 = live-DB verify:** with two linked users against local Supabase, both complete today's devotional → re-complete cleanly + each sees the other's reflection only after both reflect. **NEXT: D2 (prayers + realtime), then D3 (check-ins — fixes Bug #1, needs a new `check_ins` UPDATE-RLS migration + the same upsert `onConflict` fix), D4–D7.**
- **🟢 (2026-06-26):** **Dev build WORKS on the iOS simulator** — the fix was simply to **open the Simulator app first**, then `npx expo run:ios` (do NOT pass `--device <udid>` for a simulator — that forces a physical-device build that needs signing). The cream "Good evening" dashboard + `WaitingForPartnerCard` (invite code) render in the dev build. B3 social buttons appear in the dev build (hidden only in Expo Go); owner hasn't tapped-tested Apple/Google yet but they're available. **In flight:** (1) **bottom-nav icons** swapped emoji→Ionicons (outline inactive / filled active); a TEMP A/B is live in `app/(tabs)/_layout.tsx` — **purple chosen (2026-06-26)** — all tabs use the purple fill (filled Ionicon + label when active, outline + gray when not); A/B removed. (2) **C3 (avatar upload) — CODE DONE 2026-06-26 (R2 + DiceBear).** Tap your avatar on Profile → `app/(tabs)/profile/avatar.tsx`. **C3a presets ✓** (works in the current build, no rebuild): `src/features/profile/presetAvatars.ts` (DiceBear `open-peeps` URLs) + "Shuffle avatar" → `useSetAvatar`. **C3b upload ✓ (code):** "Upload a photo" → `expo-image-picker` (lazy-loaded) → `supabase.functions.invoke('avatar-upload-url')` → presigned PUT to R2 → public URL saved (`src/api/avatars.ts`, `useUploadAvatar`). Edge function: `supabase/functions/avatar-upload-url/index.ts` (Deno + `aws4fetch` SigV4 presign; excluded from app tsconfig). Tested (`__tests__/profile/presetAvatars.test.ts`). **⚠️ Owner to finish C3b:** (a) set 5 Edge-Function secrets — `R2_ACCOUNT_ID=957172400112fac2c7a5d075799a76a0`, `R2_BUCKET=bexhearts-avatars`, `R2_PUBLIC_BASE_URL=https://pub-468c17064e4442a0a90c1d32f00fd595.r2.dev`, plus the private `R2_ACCESS_KEY_ID` + `R2_SECRET_ACCESS_KEY` (R2 API token, Object R/W); (b) serve/deploy the function (`supabase functions serve avatar-upload-url --env-file supabase/functions/.env` locally, or `supabase functions deploy`); (c) **rebuild** the dev build (`expo-image-picker` is a new native dep). The 3 non-secret R2 values are pre-filled in the gitignored `supabase/functions/.env`; only the R2 API token's Access Key ID + Secret need pasting. Presets need none of this. **Also `00007_default_avatar.sql` (apply in Studio):** `handle_new_user()` now auto-assigns a DiceBear avatar at signup (all methods) + backfills existing null avatars — so new accounts get an illustrated avatar, not the blank purple circle.
- **Branch:** `authflow` (off `dev` off `main`). Many commits (latest `af2875a`); push when asked.
- **Stage B (Auth):** **B1 (auth foundation) is COMPLETE** — M1 encrypted `LargeSecureStore`; M2 email sign-up + 6-digit OTP confirmation (app + `confirm-signup.html`, `enable_confirmations = true`); M3 sign-in hardening + password eye toggle + `BackButton`; M4 persistent session (AppState auto-refresh + `reauthenticate` stub); M5 "last used" hint (AsyncStorage) + `SignOutLink` on onboarding; M6 route gating verified.
- **Verified in the iOS simulator:** sign-up → OTP email in Mailpit → verify → onboarding. Sign-out lives on the Profile tab (+ a link on onboarding).
- **B2 (password reset via OTP) ✓** — enumeration-safe forgot-password → code screen → set new password; recovery template `reset-password.html` + config; **Bug #3 resolved**. (Restart Supabase to load the recovery template.)
- **Dev build ✓ (2026-06-21):** CocoaPods installed, `ios/` prebuilt (`Podfile.lock` present), `package.json` scripts → `expo run:ios|android`. Daily loop: `npx expo start` → `i` opens the **dev build** (not Expo Go). Rebuild only on native-dep/config changes. RC dedupe **deferred to Stage F** (not a blocker — see STEPS infra-debt block).
- **B3 (social sign-in — Apple + Google) — WIRING COMPLETE (2026-06-21):** `@react-native-google-signin` v16 + conditional config plugin (iOS URL scheme via env); `authService.signInWithGoogle/signInWithApple` → `signInWithIdToken`; `src/features/auth/socialAuth.ts` (native token-getters + `isGoogleSignInConfigured` graceful gate); `SocialAuthButtons` on sign-in + sign-up (Apple native button iOS-only, Google when configured, App-Store-compliant, per-method "last used"); `useAuth.signInWithApple/signInWithGoogle` record `setLastUsedMethod`; tests in `__tests__/auth/socialSignIn.test.ts` + `socialAuth.test.ts`; gate green. Provider config pre-wired in `supabase/config.toml` (apple client_id `com.bexhearts.app`; google via env) — **both `enabled = false` until creds**.
- **B3 on-device verify DEFERRED (owner decision 2026-06-26):** social-auth wiring + credentials are done/committed; verifying the buttons needs the dev build, deferred to a later dev-build pass (the social buttons now **hide in Expo Go** via an `executionEnvironment === StoreClient` guard in `SocialAuthButtons`, so Expo Go dev stays clean; they auto-appear in the dev build). **Build gotcha (why it's deferred):** `pod install` succeeds, but `npx expo run:ios` keeps targeting a **physical iPhone** (a device is connected; and Xcode 26.5's `devicectl` JSON is newer than this Expo CLI parses → "Unexpected devicectl JSON version output"), and physical builds need a signing cert. **Do NOT use `--device <udid>` for a simulator — that flag forces a physical-device build.** When ready to verify natively: easiest is **Xcode → open `ios/Bexhearts.xcworkspace` → pick a Simulator destination → Run** (simulators need no signing), then `npx expo start --dev-client`; for a physical device, set Signing & Capabilities → Automatically manage signing → Team. Apple needs NO app env vars (native flow; bundle id is the audience). See `docs/B3-SETUP.md`.
- **B4 (account deletion) — DONE + HARDENED (2026-06-21):** v1 `00003` (immediate hard-delete, applied) **superseded by v2 `00005_account_deletion_grace.sql`**: **7-day grace soft-delete** (`request_account_deletion` stamps `profiles.deletion_scheduled_at`; `cancel_account_deletion` clears it; `process_due_account_deletions` is the cron-run hard-delete) + **non-destructive partner hand-off** (partner_b promoted to partner_a so the survivor keeps the couple; couple removed only when its last member leaves). `00005` DROPs `delete_my_account`. **Reactivation** on sign-in (`AuthProvider`), **PII purge** (PostHog/Superwall/RevenueCat) on sign-out, **subscription notice + store deep link** + 7-day-grace copy on the delete screen. `useAuth.deleteAccount` → `authService.requestAccountDeletion`. Tests updated; gate green. **⚠️ Owner action: apply `00005` in Studio + `supabase gen types typescript --local > src/types/database.ts`; verify grace + reactivation + partner hand-off.** **Documented follow-ups (not built):** Apple token revocation on delete (Phase 2/10, B3-blocked), pg_cron schedule for the processor (G2), server-side RevenueCat customer deletion (Phase 5).
- **Greeting fix (2026-06-21):** dashboard greeting is device-time-based via `getGreeting()` (`src/utils/greeting.ts`); tested. **Re-invite (2026-06-21):** `refreshInviteCode`/`useInviteCode.regenerateCode` + "Generate a new code" on partner-invite; surviving-partner UI lands in C2.
- **➡️ Stage B (Auth) COMPLETE** in code (pending only the owner-side B3 credential flips + on-device social verify, and applying `00005`).
- **Stage C STARTED — C1 (relationship-stage capture) DONE (2026-06-21):** migration `00004_relationship_stage.sql` (nullable `relationship_stage` `dating|engaged|married` + reserved `stage_started_on` on `couples`). New onboarding screen `app/(onboarding)/relationship-stage.tsx` + `RelationshipStageForm` between profile-setup and partner-invite; value stashed in `onboarding.store` → written onto the couple at `createCouple` (partner-invite); `onboarding_completed` moved from profile-setup → the stage screen so gating can't skip the question. Tested (`__tests__/onboarding/relationshipStage.test.ts`); gate green. **⚠️ Owner: apply `00004` in Studio + `supabase gen types typescript --local > src/types/database.ts` — `createCouple` writes the column, so onboarding errors at runtime until applied.** Open C1 decisions for owner: (a) partner_b answers the stage but inherits partner_a's; (b) narrow kill-window gap → couple created with null stage.
- **C1b (personalization + plan summary + paywall placement) — DONE (2026-06-21):** migration `00006_personalization.sql` (`profiles.growth_focus TEXT[]`). `app/(onboarding)/personalize.tsx` (`PersonalizationForm`, multi-select → profile + `onboarding.store`) → `app/(onboarding)/plan-summary.tsx` (`PlanSummary`, personalized by name/stage/focus) → CTA `triggerPaywall('onboarding_paywall')` (no-ops in dev) → partner-invite. Tested; gate green. **⚠️ Owner: apply `00006` in Studio + regen types.** Full onboarding funnel now wired: profile-setup → relationship-stage → personalize → plan-summary → partner-invite.
- **C2 (two-user partner linking) — CODE DONE (2026-06-21):** M1 collision retry (`withUniqueInviteCode` in `src/utils/invite-code.ts`, used by `createCouple` + `refreshInviteCode`); **fixed the joiner bug** — `usePartnerLink` set `partnerId=null` so the joiner was stuck in solo mode until restart, now resolves the partner via `getPartnerProfile` on link; M2 solo-mode (`couple.store.isLinked` gate + dashboard `WaitingForPartnerCard` = share/copy/regenerate invite). `createCouple` now generates the code internally (caller no longer passes one). Tested; gate green. **⚠️ Owner runs M3:** the two-user smoke test in README ("Manual smoke test — two-user partner linking"). Known gap: inviter sees the new partner only after reopening the app until couple realtime is wired (Step D7).
- **B3 social sign-in — CREDENTIALS WIRED (2026-06-23), pending on-device verify.** Apple account approved → `[auth.external.apple] enabled = true`. Google clients created; Web/iOS/Android client IDs in `.env` + `config.toml` audiences; `[auth.external.google] enabled = true`. **Owner's remaining steps:** (1) register Apple App ID `com.bexhearts.app` with the **"Sign In with Apple"** capability; (2) `export SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=<Web client secret>` (persist in `~/.zshrc`) → `supabase stop && supabase start`; (3) `npx expo prebuild --clean && npx expo run:ios` (first build pulls in the google-signin native module + iOS URL scheme); (4) verify Apple + Google buttons. Production Android needs a 2nd Google Android client with the Play App-Signing SHA-1 (Stage J).
- **➡️ Stage C (onboarding/partner-linking) COMPLETE in code (C1, C1b, C2, C3).** **NEXT: Stage D — core-loop hardening** (verify each feature against the live local DB + fix known bugs): D1 devotionals (Bug #2 upsert onConflict), D2 prayers + realtime, D3 check-ins (Bug #1 UPDATE RLS), D4 boundaries, D5 dates, D6 streak engine (Bug #4), D7 remaining realtime. See `docs/STEPS.md` Stage D. Outstanding owner tasks carried over: apply nothing new for D-start; B3 on-device social verify (optional), C3b R2 secrets+serve+rebuild, and the two-user C2 smoke test.
- **Product/billing decisions LOCKED (2026-06-21):** deletion = 7-day grace + non-destructive to partner (built); per-couple billing = stable couple-level RevenueCat alias with "keep premium until period end, then prompt" (Phase 5); subscription-not-cancelled-on-delete warning (built); Apple token revocation required pre-launch (TODO). Widgets: iOS WidgetKit first, Android separate/maybe (Phase 4B/E7 spike).

## 4. Critical environment facts (don't relearn the hard way)
- **Running on a Development Build now (Path B, since 2026-06-21).** `npx expo start` → press `i` launches the **dev build** (not Expo Go). The native modules Expo Go couldn't run (MMKV v3, RevenueCat, Superwall, native Apple/Google auth, push) now compile in. **Rebuild (`npx expo run:ios`) ONLY when the native layer changes** — adding/updating a native dep, editing `app.config.ts` native config, or touching `ios/`. JS/TS changes hot-reload, no rebuild. `react-native-worklets` was pinned to 0.5.1 for Expo Go; the dev build no longer needs that pin (re-pin if desired — non-blocking).
- **MMKV is now usable** (dev build). "Last used" (B1·M5) still uses the AsyncStorage fallback — fine, it's a non-sensitive UI hint; switch to MMKV only if you want to.
- **After any babel/native/`app.config.ts` change, rebuild (`npx expo run:ios`); for JS-only cache issues, `npx expo start -c`.**
- **Local Supabase ports:** API `55321`, DB `55322`, Studio `55323`, **Mailpit (email inbox) `55324`**. `.env` uses the **publishable key** (`sb_publishable_...`) as `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
- **Email confirmation is ON locally** → every signup needs the OTP code from Mailpit (`http://127.0.0.1:55324`). Local "from" is a default `admin@email.com`; production fixes it via ZeptoMail SMTP.
- **Tests/gate:** `npm run typecheck && npm run lint && npm test` must pass before any task is done. Migrations are **written only** (never run); owner applies them in Studio.

## 5. Locked decisions worth remembering
- Auth: Apple + Google + email, "last used" hint; persistent session; **password reset & signup confirmation use 6-digit OTP codes, NOT deep links** (resolves Bug #3).
- Email: auth via **Supabase Custom SMTP → ZeptoMail** (US `smtp.zeptomail.com`, sender `noreply@auth.bexhearts.com`); transactional via **Supabase Edge Functions + `fetch`** (Phase 6, no app package); subdomains `auth` / `notifications` / `marketing`.
- Design: **buttons & inputs have NO border radius** (square corners); brand **purple `#9849FA`** + cream `#F8F4EC`; dark mode is documented (PROGRESS Phase 8) but not built.

## 6. How to use this file
When resuming: read §1's files, then check `docs/STEPS.md`'s current-position marker. Update §3 here whenever a step completes, and refresh "Last updated" at the top.
