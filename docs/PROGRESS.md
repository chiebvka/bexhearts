# PROGRESS.md — Bexhearts Build Tracker

> ## ⚠️ Protocol for AI agents (Claude, Codex, Antigravity, etc.) and humans
>
> This file is the **single source of truth** for project state. Whenever you are asked to create a todo list, plan work, or implement anything:
>
> 1. **Read this file first.** Find which phase/item the requested work belongs to. If the request doesn't map to an existing item, add it to the right phase (or the Backlog) before starting.
> 2. **Respect the order.** If a requested task depends on an earlier unfinished item (e.g. building UI for a flow whose API is broken), flag the dependency before proceeding.
> 3. **Update statuses when you finish work** — change `[ ]`/`[~]` to `[x]`, and remove fixed items from Known Bugs. Add newly discovered bugs to Known Bugs with file references.
> 4. **Never run database migrations against any environment.** Migrations are applied manually by the owner via Supabase Studio (local and VPS). Your job ends at writing the numbered file in `supabase/migrations/`.
> 5. **Schema changes are append-only.** New numbered migration file every time; never edit applied migrations.
> 6. Functionality first, polish later: the current priority is making every flow in Phases 3–5 **and the Phase 4B engagement layer** actually work end-to-end before any visual/UI-library work (Phase 8).
> 7. **Tests ship with the work, not after.** Every feature or bugfix includes unit tests for its logic (API hooks, utils, stores, schemas) in `__tests__/`, and `npm run typecheck && npm run lint && npm test` must pass before the task is considered done. Phase 7 tracks the backfill of existing gaps; new code never adds to that backlog.
>
> Status legend: `[x]` done & verified · `[~]` partially done (see note) · `[ ]` not started

---

## What Bexhearts is

A subscription mobile app for **Christian couples** to grow their relationship and faith together. One couple = two linked users. Core loop: do something together daily/weekly, keep the streak alive.

**Positioning (locked 2026-06-10, owner decision — do not relitigate):**
- **Faith-forward, unapologetically.** Scripture-led devotionals and prayer are the center of the product, not a flavor. We are NOT a secular couples app with a Christian skin; we own the underserved "app for Christian couples" niche (vs. Paired/Couply/Love Nudge in the secular lane). A secular sibling app is a possible LATER spin-off from the same codebase + different content pack — out of scope until this app has product-market fit.
- **Beachhead audience: the dating → engaged → newlywed journey.** Functionality serves all couples; marketing, ASO, and the first content packs target these three stages with stage-specific messaging (e.g. "christian dating boundaries", "christian premarital", "newlywed devotional") all funneling into the one app.
- **Product implication:** onboarding must ask relationship stage (dating / engaged / married + duration), stored on the couple, so devotional/date/boundary content can be stage-relevant and paywalls stage-targeted. See Phase 3 item and the migration note in Phase 1.

**Auth & onboarding decisions (locked 2026-06-17, owner-approved — auth + onboarding are ONE user funnel):**
- **Sign-up:** Apple + Google + email/password. Show a "last used" hint on the previously-used method when returning signed-out. (Apple is required by the App Store whenever Google is offered.)
- **Session:** persistent / stay-logged-in (access token auto-refreshes; refresh token long-lived). Re-authenticate only for sensitive actions (delete account, change password). No forced re-login.
- **Password reset:** 6-digit email OTP **code** only — the deep-link approach is DROPPED. This is how Bug #3 is resolved (not by fixing the dead link).
- **Onboarding funnel order:** Welcome/value → Sign up → Profile basics (name) → Relationship stage → 1–2 personalization Qs → personalized plan summary → 🔓 Paywall (7-day free trial) → Partner invite/link → Dashboard (today's devotional = first "aha").
- **Paywall model:** free trial → **hard paywall (NO freemium)**. Fires after the plan-summary, before partner-link, via Superwall.
- **Billing is per-COUPLE, not per-person.** One partner subscribes/trials; the partner who joins via invite INHERITS the entitlement and must NEVER hit a second paywall. The couple is the billing unit — the `premium` entitlement is granted to whoever links into the couple.
- **Solo is a STATE, not a SEGMENT.** A single person *can* use it, but we never build/position/market for solo faith growth. The solo experience is deliberately ~70% complete: single-player-safe features work (read devotional + personal reflection, personal prayer journal, streak); two-sided features (daily-question reveal, check-in comparison, partner reflections, "praying for you", shared memories) render as **locked-until-partner** empty states (locked, NOT paywalled). One wall (paywall), one carrot (partner). No celebrated "I'm solo" path in onboarding — assume couple-intent; solo is the graceful fallback.

**Pillars — two layers:**

*Depth core (the moat — mostly built):*
1. **Daily devotionals** — scripture + reflection + a "couple action"; both partners complete it, can share reflection responses.
2. **Shared prayer journal** — either partner adds prayers; mark answered; realtime sync between partners.
3. **Weekly check-ins** *(premium)* — each partner rates emotional/spiritual/communication connection 1–5 + gratitude/growth/prayer notes; compare over time.
4. **Boundaries & temptations** *(premium)* — shared accountability commitments with action plans.
5. **Date ideas** — curated library with cost/duration/scripture tie; save, complete, rate.
6. **Streaks** — couple-level streak for consecutive daily engagement.

*Engagement layer (the daily-touch "connective tissue" — NEW, from the 2026-06-13 competitive analysis of Couple Joy + Cozy; built in Phase 4B):*
7. **Daily question (answer privately → reveal)** — a faith+relationship prompt both answer separately, then unlock each other's answer; tied to the day's devotional. *(Couple Joy's core loop, faith-themed — the single highest-leverage add.)*
8. **Home-screen widget** — verse of the day + couple streak + "partner just prayed".
9. **"Praying for you" tap** — one tap sends the partner a "🙏 praying for you right now" push.
10. **Couple challenges** — themed multi-day plans (7-day prayer challenge, 30 days of gratitude); a premium + virality lever.
11. **Milestones & countdowns** — anniversary, engagement-versary, baptism/faith milestones.
12. **Daily mood share** — one-tap emoji "spiritual + emotional weather"; feeds the widget and the praying-for-you moment. Kept deliberately lightweight so it does NOT compete with the weekly check-in (different cadence + depth).
13. **Memories timeline** — "our story" feed assembling answered prayers + completed devotionals + completed dates + photos.

*Business:*
14. **Monetization** — free tier + premium (RevenueCat entitlement `premium`, Superwall paywalls). Leaning toward gating **content volume** (full history, all challenges, unlimited daily questions, partner-comparison views, memories) over gating whole features — faith audiences pay for *more of the thing they came for* (cf. Hallow, Couple Joy's unlimited-questions paywall). See Phase 5.

**Deliberately OUT of scope (do NOT build — from the same 2026-06-13 analysis):** virtual pet/plant/home + stars economy (Cozy), generic games (Never Have I Ever / Would You Rather), in-app couple chat, distance/LDR tracking, doodle sharing. *(One narrow exception parked in Backlog: a single "faith garden" streak visual — a reward, not an economy.)*

**Release scope:**
- **v1.0 (launch):** the full depth core + the entire engagement layer above, all verified working.
- **v1.x fast-follow (first 4–8 weeks post-launch):** expansion/polish of those — more challenge packs, the faith-garden streak visual, deeper comparison views. The line exists to stop scope creeping *past* it, not to cut anything from v1.0.

**Architecture rules of the road:**
- All Supabase table access goes through hooks in `src/api/` (TanStack Query). Screens never call `supabase.from()` directly.
- Auth/session → `src/stores/auth.store.ts`; couple context → `src/stores/couple.store.ts`; both hydrated by `src/providers/AuthProvider.tsx`.
- Cross-partner data isolation is enforced by RLS using `public.get_my_couple_id()` — every couple-scoped table policy depends on it.
- Paid SDKs (RevenueCat/Superwall/PostHog) are wrapped in `src/services/` and silently no-op when env keys are placeholders, so local dev never needs them.

---

## Phase 0 — Project foundation
- [x] Expo SDK 54 + TypeScript + Expo Router scaffold (`app/`, `app.config.ts`)
- [x] Path alias `@/* → src/*`, ESLint, Jest configured
- [x] Folder architecture (api / components / features / services / stores / theme)
- [x] `.env.example` with all required vars; placeholder-detection so missing paid keys don't crash
- [x] **Initial git commit** — done 2026-06-13; pushed to `github.com/chiebvka/bexhearts` (`main`).
- [x] **CI — ✅ BUILT 2026-07-27, `.github/workflows/gate.yml`.** Two jobs. **`gate`** runs typecheck → lint → test on every push and PR, with `if: '!cancelled()'` on lint and test so one push surfaces every problem instead of one per round-trip. It runs on **placeholder** `EXPO_PUBLIC_*` values, never real secrets: that's not a compromise, it's a continuous test of the standing "paid SDKs degrade gracefully on fake keys" rule (verified locally — 403 tests pass in a stripped env). **`migrations`** enforces the two things that break when files are numbered by hand: no duplicate migration numbers, and (on PRs) **no edits to an existing migration file** — schema changes are append-only because the owner may already have run that file in Studio, and editing it makes the repo and the live DB disagree forever. Nothing in CI touches a database. **Why now:** the gate was a habit, not a gate — it only ran when someone remembered, and all three owner-found bugs of 2026-07-26 (Map/cache crash, cold-start mis-route, onboarding bypass) shipped past it.

## Phase 1 — Database & backend
- [x] Initial schema migration `supabase/migrations/00001_initial_schema.sql` — 9 tables (profiles, couples, devotionals, devotional_progress, prayers, check_ins, boundaries, date_ideas, couple_dates), `handle_new_user` trigger, `updated_at` triggers, `get_my_couple_id()` helper, `link_partner()` RPC, RLS on every table, realtime publication for prayers/devotional_progress/couples
- [x] Migration `00002_restrict_invite_lookup.sql` — invite codes only resolvable via the `link_partner` RPC
- [x] Seed data (`supabase/seed.sql`) — 7 sample devotionals, 10 date ideas
- [x] Migration `00003_delete_my_account.sql` — `delete_my_account()` SECURITY DEFINER RPC for account deletion (B4); **owner applies in Studio, then regen types** *(superseded by `00005` below)*
- [x] Migration `00005_account_deletion_grace.sql` — `profiles.deletion_scheduled_at` + `request_account_deletion()` / `cancel_account_deletion()` / `process_due_account_deletions()` (7-day grace + non-destructive partner hand-off; drops `delete_my_account`). **⚠️ Owner applies in Studio + regen types.** Needs a pg_cron schedule for the processor (Phase 6/Stage I)
- [x] Migration `00006_personalization.sql` — `profiles.growth_focus TEXT[]` (C1b "what do you want to grow in?", per-person). **⚠️ Owner applies in Studio + regen types** (the personalize screen writes it at runtime)
- [x] Migration `00007_default_avatar.sql` — `handle_new_user()` now auto-assigns a DiceBear `open-peeps` avatar (seeded by user id) so new accounts aren't a blank purple-initials circle; backfills existing null avatars. **⚠️ Owner applies in Studio.** (No type change.)
- [x] Local stack config (`supabase/config.toml`, custom ports 55321–55324, email confirmation off for dev)
- [~] TypeScript DB types — `src/types/database.ts` is hand-written to match schema; regenerate via `supabase gen types typescript --local` once local DB is up and treat generated output as authoritative
- [x] Migration `00008_check_ins_update_policy.sql` — adds the missing UPDATE RLS policy on `check_ins` (D3·M1, 2026-06-29): `USING`/`WITH CHECK (user_id = auth.uid() AND couple_id = get_my_couple_id())`, so resubmitting the weekly check-in isn't RLS-denied. Fixes Bug #1's server side (pairs with the `onConflict` code fix). **✅ Owner applied 2026-06-29** (no type change).
- [x] Migration `00009_prayers_couple_update.sql` — broadens the prayers UPDATE policy from author-only to any couple member (D2, 2026-06-29): `USING`/`WITH CHECK (couple_id = get_my_couple_id())`, so **either partner** can mark a prayer answered / archive it. DELETE stays author-only; column-level "only author edits text" is app-enforced. **✅ Owner applied 2026-06-29** (no type change).
- [x] Migration `00010_boundaries_category_and_couple_update.sql` — (D4, 2026-06-29) ADD COLUMN `boundaries.category` (nullable **freeform TEXT** — taxonomy grows with the future template library, app supplies options via `src/features/boundaries/categories.ts`) + broadens the boundaries UPDATE policy to any couple member (mirror `00009`, either partner can update/deactivate). **✅ Owner applied 2026-06-29** (regen types when convenient; `src/types/database.ts` hand-updated to match).
- [x] Migration `00011_couple_dates_custom_and_schedule.sql` — (D5, 2026-06-29) `couple_dates.date_idea_id` made **nullable** + ADD `custom_title` / `custom_description` (log-your-own dates) + `scheduled_for DATE` (planning) + `CHECK (date_idea_id IS NOT NULL OR custom_title IS NOT NULL)`. RLS unchanged (already `FOR ALL` on the couple). **⚠️ Owner applies in Studio + regen types** (adds columns; `src/types/database.ts` hand-updated so the app compiles now).
- [x] Migration `00013_journal.sql` — (E5+E6 Journal, 2026-07-04) 4 tables: `couple_milestones` (Special Days), `memories` (posts), `memory_images` (N/entry → the separate `bexhearts-memories` R2 bucket, schema ready before the upload UI), `memory_reactions` (one ❤️/🙏 + note per partner per memory). RLS: milestones/memories/images couple-managed (`FOR ALL`), reactions readable by couple + writable by own author. Adds memories/milestones/reactions to the realtime publication. **⚠️ Owner applies in Studio + regen types** (`src/types/database.ts` hand-updated so the app compiles now).
- [x] **Streak engine** — migration `00012_streak_engine.sql` (D6, 2026-06-29): `update_couple_streak()` trigger on `devotional_progress` (AFTER INSERT/UPDATE OF completed_at) + new `couples.timezone` / `grace_days_remaining` / `grace_week` columns. Rules (LOCKED): anchor = **completing the daily devotional**; **everyone in the couple** must complete it that day (1 solo / 2 linked → a solo streak "levels up" on linking); yesterday→+1, today→noop, single missed day covered by a **weekly grace day** ("grace covers you") else reset to 1; "today" computed in the **couple's timezone**. Canonical logic mirrored + tested in `src/features/dashboard/streak.ts` (`__tests__/features/streak.test.ts`). **⚠️ Owner applies `00012` in Studio + regen types** (`database.ts` hand-updated so the app compiles now).
- [x] Avatar storage — **DECISION 2026-06-26: Cloudflare R2** (not Supabase Storage, so no bucket migration). Uploads go through the `avatar-upload-url` Edge Function (presigned PUT; R2 creds are function secrets). Presets are DiceBear URLs. Both stored in the existing `avatar_url`.
- [x] Migration `00023_devotional_local_day.sql` — (2026-07-10, owner approved) `get_today_devotional()` now computes "today" in the **couple's timezone** (falls back to UTC), matching the streak engine's day boundary (00012) — the devotional no longer flips at UTC midnight (~4–5pm US west coast). Also fixes a latent 00020 bug: the rotation OFFSET applied to the whole UNION, so a `publish_date` override on a nonzero-offset day was skipped and the RPC returned no rows; branches are now separate. **⚠️ Owner applies in Studio** (function replacement, no type change).
- [x] Migration `00024_devotional_drafts_seed_2.sql` — (2026-07-10) **40 more AI-drafted devotionals** (pre-launch depth batch, owner approved): pushes the pool to 100 so the first rotation repeat lands ~100 days out; themes cover corners the first 60 didn't (distance/busy seasons, money & contentment, in-laws, technology & attention, friendship & laughter, waiting & disappointment, serving beyond yourselves, rest & celebration). Statically validated (unique titles incl. vs 00021, unique refs, valid category/focus_tags, length bounds). Generator promoted into the repo at `scripts/gen-devotionals.mjs` (was scratchpad-only). **⚠️ Owner: run 00024 in Studio → review drafts (theological review required) → approve → `SELECT public.promote_approved_devotionals();`** (same flow as 00022). NOTE: `00022_bulk_approval_for_devotionals.sql` is a deliberate one-off DATA file kept in migrations as part of the owner's ordered VPS run-list (owner decision 2026-07-10).
- [x] **Relationship stage** — migration `00004_relationship_stage.sql` (C1, 2026-06-21): `relationship_stage` (`dating`|`engaged`|`married`, CHECK) + `stage_started_on` (date, reserved for a later "how long") on `couples`, both nullable. **⚠️ Owner: apply `00004` in Studio + regen types — `createCouple` writes `relationship_stage`, so onboarding fails at runtime until applied.** Stage-tagging of `devotionals`/`date_ideas` content deferred to content work (existing `category` may suffice)
- [ ] **Engagement-layer schema** (new migration(s), for the Phase 4B features — design table-by-table when building each step, couple-scoped RLS via `get_my_couple_id()` like existing tables): `daily_questions` content + `daily_question_responses` (answer-then-reveal); `moods` (one row per user per day); `couple_milestones` (label, date, recurring flag); `challenges` content + `couple_challenge_progress` (or reuse `date_ideas.is_challenge` — decide); "praying for you" can be push-only (no table needed)
- [ ] Server-side premium enforcement (optional hardening: `is_premium` devotionals/date_ideas are currently readable by any authenticated user; gating is client-only)
- [ ] `couples.subscription_tier` is never synced from RevenueCat (needs webhook → edge function, or drop the column and rely on RevenueCat alone)

## Phase 2 — Auth & session
- [x] Email/password sign-up, sign-in, sign-out (`src/features/auth/`, `authService`)
- [x] **Email confirmation via 6-digit OTP** (B1·M2 app-side, 2026-06-18): `signUp` branches on session (present → app; absent → `app/(auth)/verify-email.tsx` → `verifySignupOtp`). Added `authService.verifySignupOtp/resendSignupOtp`, `otpSchema`, `VerifyEmailForm`; tested in `__tests__/auth/`. **Supabase side done (2026-06-18):** `enable_confirmations = true` + `[auth.email.template.confirmation]` → `supabase/templates/confirm-signup.html` (Bexhearts-branded, `{{ .Token }}` code). Restart Supabase to apply; read the code from Mailpit (:55324) in dev. (Recovery/reset template comes in B2.) Email infra decided: auth → Supabase Custom SMTP → ZeptoMail (US `smtp.zeptomail.com`, sender `noreply@auth.bexhearts.com`); local → Mailpit; transactional → Phase 6 edge functions (no app package, no app-side mail keys).
- [x] Session persistence + auth state listener + store hydration (`AuthProvider`). **Resilience fix (2026-07-04):** `getSession()` REJECTS on an invalid/expired refresh token (e.g. after a local DB reset, or a real user's token expiring) — the mount handler had no `.catch`, so `setSession` never ran, `isLoading` stayed `true`, and the app hung on the loading screen. Now the bootstrap is wrapped in try/catch (any failure → treat as signed-out → reach sign-in) and context/SDK hydration is best-effort. **Hardened again 2026-07-04:** a clean *rejection* was handled, but after a Supabase restart the client's session-recovery can *hang* (promise never settles) → still stuck. Added a **6s timeout race** on `getSession()` at boot (`withTimeout`) so the loading screen ALWAYS resolves; on timeout/reject → signed-out → sign-in. **ROOT CAUSE fixed 2026-07-04:** it was the supabase-js **auth-lock deadlock** in React Native — the default (browser Web Locks) lock can wedge; a stuck stale-token refresh held it and every later auth call (`getSession`, `signInWithPassword`) hung behind it (sign-in spun forever). Fix: pass `lock: processLock` (supabase-js's in-process lock for non-browser envs) in `src/services/supabase/client.ts`. The 6s boot timeout stays as belt-and-suspenders.
- [x] **Foreground token auto-refresh + re-auth stub** (B1·M4, 2026-06-21): `AppState` start/stopAutoRefresh wired in `client.ts` (Supabase RN pattern); `authService.reauthenticate(email, password)` re-verifies before sensitive actions (used by account deletion in B4); tested. Persistent encrypted session (survives app restart via `LargeSecureStore` + `getSession()` on mount).
- [ ] **Password autofill association (Associated Domains)** — for OS-suggested saved passwords keyed to the app, add `webcredentials:auth.bexhearts.com` + host an `apple-app-site-association` file (needs the dev build + live domain; launch-time). Basic OS "Save password?" already works via the `textContentType`/`autoComplete` hints on the forms. (Passkeys = separate future option.)
- [x] Route gating in `app/index.tsx`: unauthenticated → sign-in; no profile → onboarding; no couple → partner-invite; else → tabs
- [x] **Sign-in hardening** (B1·M3, 2026-06-18): unconfirmed-email sign-in resends a fresh OTP and routes to `verify-email` (no dead-end); sign-out cleanup verified; tests in `__tests__/auth/useAuth.test.ts`. Also added a **password show/hide eye toggle** to the shared `src/components/ui/Input.tsx` (Ionicons; covers every password field) and fixed a focus/blur prop-override bug there.
- [x] Forgot-password screen sends reset email
- [~] **Social sign-in (Apple + Google)** — **wiring complete (B3, 2026-06-21):** added `@react-native-google-signin/google-signin` v16 + a conditional config plugin (iOS URL scheme via `EXPO_PUBLIC_GOOGLE_IOS_URL_SCHEME`, omitted until set); `authService.signInWithGoogle`/`signInWithApple` → `signInWithIdToken`; `SocialAuthButtons` (native Apple button iOS-only + Google when configured, App-Store-compliant pairing, per-method "last used" tag) wired into sign-in + sign-up; `useAuth.signInWithApple/signInWithGoogle` orchestrate token→exchange→route and swallow cancellations; `socialAuth.ts` degrades gracefully (`isGoogleSignInConfigured()` hides the button until the Web client ID is real). Tested in `__tests__/auth/socialSignIn.test.ts` + `socialAuth.test.ts`. **Credentials wired 2026-06-23:** Apple Developer account approved → `[auth.external.apple] enabled = true` (client_id `com.bexhearts.app`). Google clients created (project `bexhearts`, no. `236343232119`): Web/iOS/Android client IDs in `.env` (`EXPO_PUBLIC_GOOGLE_*`, public) + all three listed as audiences in `[auth.external.google] client_id` (now `enabled = true`); Android matched by package + debug SHA-1. **Pending (owner) → verify on dev build:** (1) register the Apple **App ID + "Sign In with Apple" capability** in the dev portal; (2) `export SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=<Web client secret>` then `supabase stop && supabase start`; (3) `npx expo prebuild --clean && npx expo run:ios` (picks up the google-signin native module + iOS URL scheme); (4) tap Apple + Google on the dev build. Apple required by the App Store whenever Google is offered.
- [x] **Apple token revocation on account deletion — ✅ BUILT 2026-07-30 (gate green 416 tests). ⚠️ OWNER: apply `00037_apple_credentials.sql` + set four Apple secrets (`docs/APP_STORE.md` §6b).** The last hard App Store blocker, open since B4 in June. **Why a table was unavoidable:** revocation needs a token, and the only thing the device receives at sign-in is an `authorizationCode` that **expires in ~5 minutes** — long gone by the time someone deletes their account months later. So the code is exchanged for a long-lived **refresh token at sign-in** and stored until needed. `apple_credentials` is RLS-on-with-zero-policies (the `devotional_drafts`/`comp_access` pattern) so no client can read it — not even its owner; only the `apple-revoke` edge function touches it, via the service role, and it `ON DELETE CASCADE`s with the profile so no token outlives its account. **The edge function** (`supabase/functions/apple-revoke/`) builds Apple's required ES256 client-secret JWT with Web Crypto (no JWT dependency), handles a `.p8` pasted with real newlines *or* escaped `\n`, and has three actions: `store` (user JWT, at sign-in), `revoke` (user JWT, at deletion), `sweep` (service role, retries revokes that failed because Apple was unreachable). **A user JWT can only ever act on itself** — the user id comes from the verified token, never the body, so there is no way to ask it to revoke someone else. **Revocation fires at deletion REQUEST, not at the end of the 7-day grace:** an App Review tester taps "Delete account" and expects the Apple link gone right then, and the grace window is our own product decision Apple knows nothing about. Revoking early is harmless — signing back in during grace cancels the deletion and stores a fresh token. **Client** (`src/api/appleRevoke.ts`) is best-effort by construction: neither call can throw, revoke runs *before* `requestAccountDeletion` while the session is still valid, and `useAuth` wraps it in its own try/catch as belt-and-braces — deleting your account must never fail for a reason the user can't act on. 13 tests pin the ordering, the never-throws contract, and that no user id is ever sent. **Degrades to a documented no-op until the secrets exist** (returns `{configured:false}`), so nothing breaks before the owner sets them — which is also the trap: it *looks* healthy. Verify per §6b.
- [x] **"Last used" sign-in hint** (B1·M5, 2026-06-21): persists the last-used method via AsyncStorage (`src/features/auth/lastUsedMethod.ts` — non-sensitive, deliberately NOT MMKV); `SignInForm` shows a hint. Email-only for now; per-method tags land with the social buttons (B3). Also added a **`SignOutLink`** to onboarding (welcome + profile-setup) so a signed-in-but-not-onboarded user can sign out — the primary sign-out is the button on the Profile tab.
- [x] **Password reset via 6-digit OTP code** (B2, done 2026-06-21): forgot-password → `requestPasswordReset` (**enumeration-safe** — navigates to `reset-password` regardless of whether the account exists) → enter code + new password → `verifyRecoveryOtp` (`type: 'recovery'`) → `updatePassword`. Recovery template `supabase/templates/reset-password.html` + `[auth.email.template.recovery]`. **Resolves Bug #3** (deep link dropped). Tested in `__tests__/auth/`.
- [x] Secure session storage — **done 2026-06-18 (B1·M1):** `LargeSecureStore` (AES-256 key in SecureStore, ciphertext in AsyncStorage) wired into the Supabase client at `src/services/supabase/secureStorage.ts`. Uses `expo-crypto` for the random key (Expo-Go-safe) instead of `react-native-get-random-values`. Tested in `__tests__/services/largeSecureStore.test.ts`. Note: switching the storage backend logs out any pre-existing dev session once.
- [x] **Account deletion** (B4 + hardening, 2026-06-21) — App Store requirement. **v1** (`00003`, applied): immediate hard-delete RPC. **v2** (`00005_account_deletion_grace.sql`, ⚠️ owner must apply + regen types): **7-day grace soft-delete** + **non-destructive partner hand-off**. `request_account_deletion()` stamps `profiles.deletion_scheduled_at = now()+7d`; `cancel_account_deletion()` clears it; `process_due_account_deletions()` (backend/cron, `service_role`) hard-deletes the due ones, promoting partner_b→partner_a first so the **remaining partner keeps the couple + shared data** (couple removed only when its last member leaves; the leaver's own authored rows still cascade — GDPR-aligned). `00005` DROPs the destructive `delete_my_account()`. **Reactivation:** `AuthProvider.bootstrapUserContext` calls `cancel_account_deletion()` + toasts if the user signs back in within the window. **PII purge:** `SIGNED_OUT` now also resets PostHog + Superwall + RevenueCat (`logOutRevenueCat`). UI: Settings → "Delete Account" → `app/(tabs)/profile/delete-account.tsx` — re-auth for email users (social skip), **subscription notice + store-management deep link**, 7-day-grace copy, destructive confirm → `authService.requestAccountDeletion()` → sign out → sign-in. Tested in `__tests__/auth/deleteAccount.test.ts`. **TODOs:** (a) **Apple token revocation on delete** (App Store blocker once Apple sign-in ships — edge function to Apple's revoke endpoint, see Phase 2/Phase 10); (b) **schedule `process_due_account_deletions()`** via pg_cron (Phase 6/Stage I); (c) **server-side RevenueCat customer deletion** for full GDPR (Phase 5). **Owner: apply `00005` in Studio; verify grace + reactivation + partner hand-off in Studio.**

## Phase 3 — Onboarding & partner linking (the make-or-break flow)
- [x] Welcome → profile setup (name, denomination) → sets `onboarding_completed`
- [x] Invite code generation (collision-safe charset, 48h expiry) + couple row creation (`useInviteCode`)
- [x] Partner linking via `link_partner` RPC with self-link/already-linked guards (`usePartnerLink`)
- [x] Share/copy invite code UI
- [x] **Invite share format + web landing (DECIDED + site half SHIPPED 2026-07-18):** the native share sheet stays the mechanism (user picks SMS/WhatsApp/Mail themselves — no separate email flow built). The shared link `https://bexhearts.com/invite/<code>` used to 404; the landing repo now serves **`/invite/[code]`** (formatted code, 3 join steps, 48h-expiry note, noindex) + a codeless `/invite` fallback, with store buttons auto-appearing once `appLinks` in the site's `lib/site.ts` gets real URLs. No app-side DB change needed (codes stay resolvable only via `link_partner`, 00002). **Deferred deep-linking (Branch/App Clip auto-fill after install) = post-launch.**
- [~] **End-to-end verification with two real users against local Supabase** (C2, 2026-06-21): code is ready — **fixed the joiner bug** (`usePartnerLink` set `partnerId=null`, leaving the joiner stuck in solo mode until restart; now resolves the partner via `getPartnerProfile` on link). **Owner runs the two-user smoke test** (README "Manual smoke test — two-user partner linking"). Known gap: the inviter (A) must reopen the app to see the new partner until couple realtime is wired (Step D7).
- [~] **Solo-mode behavior** (DECISION 2026-06-17 — solo is a STATE, not a segment): when `partner_b_id` is null, land in "solo mode" — single-player-safe features work; two-sided features render as locked-until-partner empty states (locked, NOT paywalled); gentle resend-invite nudges. **Done (C2, 2026-06-21):** `couple.store.isLinked` drives gating; the dashboard shows a **`WaitingForPartnerCard`** when solo (share / copy / regenerate the invite code = the resend nudge). **Remaining:** apply the locked-until-partner empty state to each two-sided feature *as it's built* (partner-reflection reveal D1·M3, check-in comparison D3·M3, daily-question reveal E1, praying-for-you E3, memories E6) — reuse the same `isLinked` gate.
- [x] Invite code collision retry (C2·M1, 2026-06-21): `withUniqueInviteCode` (`src/utils/invite-code.ts`) regenerates + retries on Postgres unique_violation (23505); used by `createCouple` + `refreshInviteCode`. Tested in `__tests__/utils/invite-code.test.ts`.
- [~] **Re-invite after a partner leaves** — data layer added 2026-06-21: `refreshInviteCode(coupleId)` (`src/services/supabase/database.ts`) + `useInviteCode.regenerateCode`; surfaced as "Generate a new code" on the partner-invite screen (also covers expired onboarding codes). **Remaining:** the surviving-partner-facing re-invite entry point lives in the C2 solo-mode empty state (a left couple has `partner_b_id` NULL + an expired code → survivor re-invites from the app).
- [x] **Avatar upload + presets (C3, 2026-06-26)** — `app/(tabs)/profile/avatar.tsx` (tap your avatar on Profile): **C3a presets** = cyclable DiceBear `open-peeps` avatars (`src/features/profile/presetAvatars.ts`, no assets, Expo-Go-safe, works in the current build) → saved to `avatar_url`; **C3b photo upload** = `expo-image-picker` (lazy-loaded) → `avatar-upload-url` Edge Function → presigned PUT to **R2** → public URL saved. `src/api/avatars.ts` (`useSetAvatar`, `useUploadAvatar`); tested (`__tests__/profile/presetAvatars.test.ts`). **⚠️ Owner for C3b:** set the 5 R2 Edge-Function secrets, serve/deploy `avatar-upload-url`, and **rebuild** (`expo-image-picker` is a new native dep). Presets need neither.
- [x] **Relationship-stage question in onboarding** (C1, 2026-06-21): new screen `app/(onboarding)/relationship-stage.tsx` + `RelationshipStageForm` (one screen, dating/engaged/married) sits between profile-setup and partner-invite. Captured into `onboarding.store`, written onto the couple at `createCouple` (partner-invite). `onboarding_completed` moved from profile-setup → the stage screen so gating can't skip the question. Tested (`__tests__/onboarding/relationshipStage.test.ts`). "How long" (`stage_started_on`) deferred (no date-picker dep yet). **Open decisions flagged to owner:** (a) joining partner (partner_b) answers the stage but it's ignored — they inherit partner_a's; (b) narrow gap if the app is killed between the stage screen and couple creation → couple created with null stage. Depends on `00004` being applied.
- [x] **Onboarding funnel order** (DECISION 2026-06-17): Welcome/value → Sign up → Profile (name) → **Relationship stage ✓ (C1)** → **Personalization ✓ (C1b)** → **Plan summary ✓ (C1b)** → **🔓 Paywall trigger placed ✓ (C1b, no-ops in dev)** → Partner invite/link → Dashboard. Auth + onboarding are one UX funnel (Stages B+C). Full funnel order is now wired (`profile-setup → relationship-stage → personalize → plan-summary → partner-invite`). **Remaining is paywall *enforcement* (Stage F), not placement.**
- [ ] **Expand onboarding into a personalization quiz + richer plan reveal** (DECISION 2026-06-29 — research-backed: longer, personalization-driven onboarding out-converts minimal; Noom/Duolingo pattern). Grow C1b's single growth-focus question into a **short couple quiz (4–6 consequential Qs** with a progress bar: time together, biggest area to grow, current season/challenge, connection cadence, optionally love languages) → a **richer personalized plan reveal** ("your couple's 90-day journey" + sample devotional/question titles) → paywall. Rules: every question must map to real personalization (no dead-end friction), one-tap answers, ~60–90s; **inviter takes the quiz, the joining partner INHERITS the plan and skips it + never sees the paywall**. Pairs with F1 (the plan reveal is the paywall's setup). Enhances the existing C1/C1b funnel.
- [x] **Personalization questions + plan summary** (C1b, 2026-06-21): `app/(onboarding)/personalize.tsx` (`PersonalizationForm`, multi-select "what do you want to grow in?" → `profiles.growth_focus` via `00006`, also stashed in `onboarding.store`) → `app/(onboarding)/plan-summary.tsx` (`PlanSummary`, personalized by name + stage + focus) → CTA triggers the Superwall paywall (`triggerPaywall('onboarding_paywall')`, no-ops in dev) → partner-invite. Tested (schema + store in `__tests__/onboarding/relationshipStage.test.ts`). Depends on `00006` applied.

## Phase 4 — Core features (all CRUD wired; none verified against a live DB)
- [x] **Devotionals** — today's devotional, history (30 days), detail screen, complete-with-reflection (`src/api/devotionals.ts`). **D1·M1 (2026-06-29): Bug #2 fixed** — `useCompleteDevotional` upsert now passes `onConflict: 'devotional_id,user_id'`, so re-completing updates instead of throwing; tested in `__tests__/api/devotionals.test.ts`. **D1·M3 (2026-06-29): partner-reflection reveal** — each partner reflects privately; you see your partner's reflection only once BOTH have reflected (`src/features/devotional/reflectionReveal.ts` pure state machine → `PartnerReflection` component on the devotional screen), gated by `couple.store.isLinked` so solo couples get a locked-until-partner state (not a paywall); tested in `__tests__/features/devotionalReveal.test.ts`. **Remaining D1·M2 = owner live-DB verify** (both partners complete today's devotional against local Supabase; confirm re-complete + reveal).
- [x] **Prayers** — list/create/update/delete, answered toggle, archive flag (`src/api/prayers.ts`). **D2 (2026-06-29):** realtime wired (`usePrayersRealtime` → `subscribeToPrayers` invalidates on partner add/answer/archive, cleanup on unmount; `__tests__/api/prayers.test.ts`); prayers screen now a `SectionList` split **Praying / Answered** via `partitionPrayers` (`__tests__/features/prayerPartition.test.ts`), answered prayers move out of the active list; **archive** action + **answered celebration** (haptic + toast). **Prayer-flow decisions LOCKED (owner, 2026-06-29):** (a) lifecycle = `Active → Answered → Archived`, **no time-based expiry**; (b) **either partner** can mark answered/archive (migration `00009`), author-only for text-edit + hard-delete; (c) staleness handled later by a **gentle triage nudge** ("Still praying for this?"), not expiry; (d) reminders = **one daily prayer digest** folded into the daily touch (Phase 6), never per-prayer pings. **Deferred (v1.x / later phases):** praise-report/update thread on a prayer (needs a `prayer_updates` table), the triage nudge + digest (need Phase 6 sending infra), answered-prayer → Memories timeline (E6). **Remaining D2 = owner live-DB verify** (two devices: add on A appears live on B; either partner answers/archives). 
- [x] **Check-ins** — weekly form (3 ratings + 3 text fields), history of 12 weeks (`src/api/check-ins.ts`). **D3 (2026-06-29): Bug #1 fixed** — `useSubmitCheckIn` upsert now passes `onConflict: 'couple_id,user_id,week_of'` (D3·M2) + new UPDATE RLS policy (`00008`, D3·M1), so resubmitting the same week updates instead of throwing; tested in `__tests__/api/check-ins.test.ts`. **D3·M3: partner comparison** — `src/features/check-in/comparison.ts` state machine (`locked`/`await-self`/`await-partner`/`revealed`) → `CheckInComparison` card on the Connect hub (`app/(tabs)/connect/index.tsx`), `isLinked`-gated, reveals both partners' ratings only once BOTH submit; new `useThisWeekComparison` hook; tested in `__tests__/features/checkInComparison.test.ts`. **Remaining = owner live-DB verify** (submit + resubmit same week; comparison reveals after both submit).
- [x] **Boundaries** — list/create/update with type boundary|temptation (`src/api/boundaries.ts`). **D4 (2026-06-29, "verify + polish only"):** screen split into two registers via `partitionBoundaries` — **Boundaries** (proactive shared covenants) vs **Temptation plans** (grace-framed: "with your partner beside you — no scores kept"); added a **category** chip picker (distinct taxonomy per type, `categories.ts`) + category badge; **deactivate** action (Retire/Resolve → `is_active=false`); **either partner** can update/deactivate (migration `00010`). Tested in `__tests__/features/boundaries.test.ts`. **Boundaries product decisions LOCKED (owner, 2026-06-29):** (a) keep the `boundary` vs `temptation` split — different emotional registers, different mechanics; (b) **gamification = boundaries get a positive "days-honored" streak (wired AFTER the D6 streak engine), temptations get NO relapse-breaking streak** — reward honesty/help-seeking with a never-resets "victories" count instead (shame-avoidance); (c) `category` added now to future-proof the template library + UGC packs. **Marketing:** boundaries = the UGC/acquisition engine (shareable advice → each template = a Reel); temptations = the depth/retention/testimonial engine (never an ad unit). **Deferred to their own steps:** ~~curated stage-aware template library~~ (v1 BUILT below), **mutual-agreement handshake** (propose→accept `agreed_at`), **"I'm struggling now" accountability tap** (needs push E3/Phase 6), boundary review during the weekly check-in. **Remaining D4 = owner live-DB verify** (create both types with a category; partner sees them; either partner can deactivate). **TEMPLATE LIBRARY v1 + FORM SIMPLIFICATION BUILT (owner approved 2026-07-10; sim-verified, logs clean):** (1) **stage-aware starter templates** — `src/features/boundaries/templates.ts`, **client-side constants, no migration** (the category column is freeform): 18 boundary templates (every category covered, stage-tagged where it matters — dating gets "honest about what we can afford", married gets "budget together monthly") + 6 grace-framed temptation templates w/ action plans; rendered as a horizontal **"Start from a template"** card rail in `BoundaryForm` (temptation register: "Common struggles — tap one that fits"), filtered by the couple's `relationship_stage` (`useMyCouple`) + the selected category chip; tapping prefills title/why/plan + selects the category. Each boundary template doubles as a marketing content seed (§2.2 UGC-engine decision). (2) **Form simplification:** "Why is this important?" now labeled **(optional)**; the **Action plan field is REMOVED for plain boundaries** (temptation-plan machinery only — boundary = statement + why; submit sends `action_plan: null`); **category-aware title placeholders** (`getTitlePlaceholder` — picking Finances shows "e.g. Purchases over $100 get a quick chat"); the form is keyed by register in `app/modal/boundary-form.tsx` so switching tabs resets state. Tested in `__tests__/features/boundaries.test.ts` (template validity/coverage/filtering + placeholders). **PRE-LAUNCH SCOPE DECISION (owner, 2026-07-10):** boundaries pre-launch = this; **handshake + gamification wiring (days-honored streak / victories count) = post-launch queue.** 
- [x] **Dates** — browse ideas by category, save to couple, complete with rating/notes (`src/api/dates.ts`). **D5 (2026-06-29) — closed the broken loop + full flexibility (owner: "combine all 3, nothing forced"):** the couple-dates side had NO screen (you could save but never see/complete). Added an **Ideas | Our Dates** segmented view on the dates tab; **Our Dates** groups by **derived status** Planned/Saved/**Memories** (`getDateStatus`/`partitionCoupleDates` — no status column) with mark-done + remove. **Custom / log-your-own dates** (`useCreateCustomDate`; `date_idea_id` nullable via `00011`). **Scheduling** via JS-only quick presets (`SchedulePicker`/`getSchedulePresets` — "This weekend / Next Friday…", deliberately no native date-picker dep). **Memory-first completion** modal (lead with the note, rating secondary → feeds E6). **Discussion questions** (seeded but previously unrendered) now shown on the idea detail. Tested `__tests__/features/dateHelpers.test.ts`. **Dates decisions LOCKED (owner, 2026-06-29):** all-of (loop + custom + scheduling); memory-first *capture* now, unified **timeline deferred to E6**; stage-aware content **deferred to J2** (category filter suffices); **date photos are a fast-follow** — they reuse the avatar R2 + `expo-image-picker` pipeline (C3b), which needs the owner's R2 secrets + a rebuild, so completion captures rating+note now and photo later. **Remaining D5 = owner live-DB verify** (after applying `00011`: save a library date, create a custom one, schedule one, complete one with rating+note; partner sees them). 
- [x] **Dashboard** — greeting, both avatars, streak counter, quick actions, today's devotional card. *(2026-06-21: greeting now tied to the device clock via `getGreeting()`; tested.)* **D6 (2026-06-29):** `StreakCounter` now reads the **live** streak (`useMyCouple`, refreshed after devotional completion) and shows the **"waiting on your partner" nudge** (`getStreakDayState` → complete / waiting-partner / waiting-you) — the core re-engagement mechanic.
- [ ] **Smoke-test every flow above against local Supabase** and fix what breaks (RLS denials, column mismatches, etc.)
- [x] Wire up realtime — all three channels now wired. `subscribeToPrayers` via `usePrayersRealtime` on the prayers screen (D2); **`subscribeToDevotionalProgress` + `subscribeToCoupleUpdates` via `useCoupleRealtime` (D7, 2026-06-29)**, mounted once in `app/(tabs)/_layout.tsx` → a partner's devotional completion (reflection reveal + streak) and any couple-row change reflect live on the other device. Tested `__tests__/api/coupleRealtime.test.ts`.
- [x] Partner visibility moments — **devotional reflection reveal DONE (D1·M3)** + **check-in comparison DONE (D3·M3)**, both 2026-06-29, both `isLinked`-gated and reveal-once-both-submit. (Daily-question reveal E1 uses the same pattern when built.)
- [ ] Empty states verified for all lists (components exist; verify with real empty DB)

## Phase 4B — Engagement & connection layer (NEW — competitive analysis 2026-06-13; none built yet)
> The "daily-touch" features that give Bexhearts Couple-Joy/Cozy-level stickiness on top of the depth core. All v1.0. Each needs: schema (Phase 1 engagement-layer migration), an `src/api/` hook, UI, and tests. Build order is set in `docs/STEPS.md`.
- [ ] **Daily question (answer privately → reveal)** — same daily faith+relationship prompt to both; answer separately; unlock each other's answer once both submit. Reuses the partner-visibility pattern from Phase 4. *Highest-leverage add (Couple Joy's core loop).*
- [ ] **Daily mood share** — one-tap emoji per partner per day; lightweight; feeds the widget + praying-for-you. Keep distinct from the weekly check-in (different cadence/depth).
- [x] **"Praying for you" tap (E3) — ✅ BUILT 2026-07-19 with G2:** one-tap on the Prayers screen + auto-notify when a prayer-focus session ends; lands in the partner's inbox/bell instantly (OS push rides the same call once tokens exist on the dev build).
- [ ] **In-app Bible reader** (NEW — owner ask, 2026-07-04). A Bible in the **Grow** tab (book/chapter nav + verse display + **version switcher**), with the devotional's scripture reference tappable → opens the passage (ties Grow together). **Licensing is the crux:** free/public-domain versions (WEB default + KJV) can be bundled/free-API now; copyrighted versions (NIV/ESV/NLT/CSB) need API.Bible/ESV-API **licensing** → ship those later as a **premium** differentiator. Dedicated feature; iron out (offline bundle vs API, versions, bookmarks/highlights) before building.
- [ ] **Onboarding personalization payoff (reality check, 2026-07-04):** the stage + growth-focus questions are currently captured but only personalize the plan-summary *copy* — they do NOT yet filter actual content (seed devotionals/dates are placeholder). Owner rightly noticed they feel like "questions for asking's sake." **Fix = wire the payoff:** when real stage/focus-aware content packs land (J2) + the onboarding-quiz expansion (below), answers must drive which devotionals/dates/challenges surface + paywall targeting. Until then, don't add MORE questions than pay off.
- [ ] **Prayer focus / "prayer time"** (NEW — owner idea from the "Prayer Lock" app, 2026-07-04). Turns the prayer *list* into a *practice*: a distraction-free **in-app full-screen focus session** (timer + your + partner's active prayers + gentle guidance), entered from Connect → Prayers ("Pray now"); finishing can fire the **"praying for you" partner notification** (ties to E3). **DECISIONS LOCKED:** (a) **focus session first; AI-generated prayers = premium v1.x add** — AI deferred because prayers are *sensitive religious data* (needs a server-side proxy + consent + privacy disclosure + a theological-safety pass; cheap model e.g. Haiku via edge function); (b) **in-app full-screen focus, NOT OS-level phone locking** (true app-blocking via iOS Family Controls/Screen Time = a heavy, Apple-gated later spike). Differentiator + premium justifier + App-Store digital-wellbeing angle. **BUILT (2026-07-05): the full focus session + AI layer** — `compose-prayer` edge fn (Claude Haiku `claude-haiku-4-5`, key server-side, consent enforced server-side, crisis guard, one-generation-per-prayer cache in `00016`), `app/modal/prayer-focus.tsx` (3/5/10-min session, keep-awake, verse + composed prayer per screen, one-time consent sheet, Amen logs a `prayer_session` activity), hybrid IA (prayers screen Ours|Mine + Pray now; Grow "Prayer time" card). **Streak surface (owner picked A+D, 2026-07-04):** upgraded Home StreakCounter (week dots + grace chip + trackables) → **Us hub** (`app/modal/us-hub.tsx`: hero streak, 12-week heatmap, per-activity streaks, badges) fed by the new `activity_log` (00016; written by devotional/check-in/journal/prayer-session mutations). ⚠️ Owner applies `00016` + serves all functions. Remaining: "praying for you" partner ping (needs push/G1), garden companion (option C) as a later premium delight. **UPDATE (2026-07-04g): foundation BUILT — personal vs shared prayers** (`00015_prayer_visibility.sql` ⚠️ owner applies BEFORE creating prayers: `prayers.is_private` + RLS hides partners' personal prayers; `PrayerForm` Shared/Personal selector, "Personal" badge). **Owner re-confirmed the AI layer (2026-07-04):** edge-function proxy → cheap model (DeepSeek or Haiku) analyzes the request → ties ONE Bible verse → generates a prayer to pray during the focus session; key server-side only; consent + privacy disclosure + theological-safety pass required before shipping. Focus session = the next build.
- [x] **Long-distance couples mode (E11) — ✅ BUILT 2026-07-25 (design round run with the owner same day, all recommended options locked; gate green 293 tests; onboarding/dates/profile sim-verified, logs clean). ⚠️ OWNER: apply `00031_ldr_mode.sql`** (until then: Virtual chip filters to empty, LDR toggle errors on save — everything else degrades fine). **Design locked:** LDR captured via quiz + Profile toggle; quiz expansion = **just the LDR question this round** (payoff rule — more questions wait for J2 content packs); virtual dates = `is_virtual` FLAG (cross-category, not an 8th category) + curated tag pass + new seeds; clock + countdown both shipped. **Ship list:** migration `00031` — `couples.is_long_distance`, `date_ideas.is_virtual`, tags 6 existing call-friendly ideas, seeds **12 purpose-built virtual ideas** (watch-together, video-call dinner, pray-across-the-miles, plan-the-next-visit…; pool → ~362); **`profiles.timezone` needed NO migration** (existed since 00001 defaulted 'America/New_York', never written — `useStampTimezone` in the tabs layout now stamps the real device zone and re-stamps on travel). App: LDR question on the relationship-stage screen ("Together nearby / Long-distance", nearby default) → stashed in onboarding.store → written at `createCouple`; Profile → **"Your relationship" → Long-distance mode** switch (`useSetLongDistance`, either partner); **Virtual 💻 filter chip** on date Ideas (`buildIdeaFilters` — leads for LDR couples, trails otherwise; `useDateIdeas('virtual')` filters the flag); **"their time" clock** on Home ("AJ & Bexoni · 3:42 PM their time" via `partnerTimeLabel` — hidden when clocks match, tz-name-safe); **✈️ Next visit** milestone preset (rides the existing countdown machinery). Pure helpers + tests in `src/features/ldr/ldr.ts` / `__tests__/features/ldr.test.ts`. Privacy: timezone clarifier added same pass.
- [x] **LDR round 2 (E12) — ✅ BUILT 2026-07-26 (gate green 315 tests; sim-verified). ⚠️ OWNER: apply `00032_westernmost_streak_anchor.sql` + `00033_global_date_ideas.sql`.** Owner reviewed E11 and found the toggle only controlled chip ordering — everything else was timezone-driven or ungated. Four fixes, all owner-decided: **(1) Virtual-first with a visit split** — for an LDR couple the unfiltered list becomes "While you're apart" (virtual) then **"For your next visit"** (everything else). Co-located ideas are SUNK, never hidden: LDR couples do meet, and that's exactly when they need the hiking. Picking a category chip returns a flat list — a specific question gets a direct answer. **(2) Streak anchor = the WESTERNMOST partner** (`00032`: `tz_offset_seconds` + `recompute_couple_timezone` + a trigger on `profiles.timezone`, backfilled). Owner originally asked to anchor to the subscription payer; rejected after the agent flagged that it needs F2 to even know who paid, doesn't exist during trials, and is arbitrary in a way users feel — an eastern anchor ends the western partner's day at 6pm their time. Westernmost is never harsh (their midnight lands last, so both get a full local day), needs no billing data, and can only be more forgiving than the old creator-based rule. **(3) The clock is real:** partner's local time + 🌙 when they're inside **their own quiet-hours setting** (fallback 10pm–7am) — descriptive, never "don't text them". **(4) ✈️ Next visit countdown** on Home ("2 days until you're together 💜"), ungated because couples apart for a work trip want it too; tapping opens the Journal. Pure helpers + 16 tests in `src/features/ldr/`. **Bug caught in verification: the milestone mutations only invalidated the timeline key, so setting a visit wouldn't surface the countdown until the 5-minute staleTime expired — all three now invalidate `journal.all`.**
- [x] **Global date ideas — ✅ SEEDED 2026-07-26 (`00033`, ⚠️ owner applies).** Owner: the 350-idea library reads as "first world" — it assumes a car, disposable income and Western infrastructure (kayak rental, balloon festivals, dog parks, fall foliage). 59 new ideas rooted in **West Africa, East Africa, Latin America, South Asia, Southeast Asia, MENA** plus ideas that work literally anywhere: Market Run One Dish · Last Stop and Back · Light-Out Worship · Sobremesa · Chai Stall Theology · Rooftop Sunset Psalm · Hymn Exchange · Carry Someone's Load · Prayer Walk Around the Block. **38 free / 19 at `$` / 2 at `$$`**, no car assumed, faith-centred, non-carnal, no alcohol. Deliberately NOT a separate "international" section — they sit in the same pool, because the genuinely universal ideas are often *more* interesting than the Western ones and appeal to first-world couples too (owner's explicit goal).
- [x] **Date ideas by country (E13) — ✅ BUILT 2026-07-26 (gate green 329 tests; chip sim-verified). ⚠️ OWNER: apply `00034_date_idea_country_tags.sql`** (until then every chip reads "🌍 Anywhere" — degrades cleanly). `date_ideas.country_tags TEXT[]` (GIN-indexed; **empty = global**, so the 362 pre-existing ideas need no backfill and read as universal, which they are) + `context_note TEXT`; the 00033 ideas tagged by region with explainers written for a reader who has never been there (danfo/trotro, harmattan, sobremesa, monsoon, simbang gabi, sari-sari, jollof). **Rankings needed no new rating infrastructure** — `couples.country_code` (00017) + `date_idea_ratings` (00018) already existed, so `get_date_idea_country_stats()` is just a country-aware aggregate (SECURITY DEFINER, returns aggregates only — never review text or who rated, same contract as 00018). UI: **`CountryChips`** on the idea detail — RN has no hover, so the "tooltip" is a tap opening a sheet with the context note, "★4.6 from 12 couples in Nigeria" (or an honest "you could be first"), and **"See more ideas from Nigeria"** → `/dates?country=NG` (`country:XX` filter via `.contains('country_tags', …)`, with its own dismissible chip since it arrives by deep link). **Passport 🌍 badge** in the Us hub (3 distinct countries from COMPLETED dates; global ideas don't count — the badge is about reaching outside your own context). **Deliberately a badge, NOT a prize:** anything prize-shaped trips the rewards-eligibility questions parked in ToS §7 for the attorney. Unknown country codes degrade to a 📍 chip rather than breaking the row.
- [x] **F2 enforcement — ✅ BUILT 2026-07-26 (gate green 329 tests).** **(a) Per-couple RevenueCat alias:** the RC app-user id is now `couple_<id>` (`revenueCatAppUserId`), not the user id — the subscription is billed per couple ("one plan, two hearts"), so **a purchase by either partner entitles both**. Solo users fall back to their own id and are **re-identified on linking** (`usePartnerLink`) and at couple creation (`useInviteCode`), so a solo subscriber carries their entitlement into the couple; entitlement queries are invalidated at the same moment. **(b) Gating — OWNER DECISION 2026-07-26: lock EVERYTHING behind the paywall**, since every couple gets the 3-day trial regardless. Implemented as an app-level gate in the tabs layout via pure `resolveAccess()`. **Four carve-outs, each load-bearing:** `(auth)` (you can't subscribe before signing in) · **`(onboarding)` incl. partner-linking — without this an invited partner hits the paywall and can NEVER join, breaking both the invite loop and per-couple billing** · `profile` (Restore Purchases + account deletion; Apple requires deletion reachable in-app) · the paywall itself. The paywall screen hides its back button when it's acting as the hard gate and offers sign-out as the deliberate way off. **Never gates when RevenueCat has no real key** (Expo Go / dev / a misconfigured build) — otherwise the app would lock permanently with no way to buy. **⚠️ OWNER RISKS TO NOTE: (1) a fully-paywalled app needs a working DEMO ACCOUNT supplied to App Review or it gets rejected; (2) a store trial requires a payment method up front, so every user commits a card before seeing anything — a real top-of-funnel cliff worth watching in the first cohort.** Real sandbox purchase verification still rides J1/F3 (dev build).
- [x] **i18n — DECIDED 2026-07-26: LAUNCH ENGLISH-ONLY, retrofit later if the data justifies it.** Owner asked whether iOS/Android/the stores auto-translate an app — **they do not.** Neither OS translates your UI strings; Apple never machine-translates a store listing (you supply each localization); Google Play machine-translates the *listing* only, never the app. What IS already automatic and working: times/dates via `Intl.DateTimeFormat` (the partner clock), store prices in local currency via StoreKit/RevenueCat, system dialog *buttons* (but not the permission purpose strings, which are ours), and AI-composed prayers already replying in the user's language (E9). **Market read:** English is official/dominant in church contexts across Nigeria, Ghana, Kenya, India and the Philippines, so those markets are reachable today; **Latin America is where English-only bites** (Spanish + Brazilian Portuguese) — exactly where the 00033 ideas are most at home. **Retrofit is mechanical, not architectural** (pull hardcoded strings into a translation file — same shape as the dark-mode codemod over 54 files), so deferring is cheap to reverse. **Cheap interim win: localize the STORE LISTING into Spanish + Portuguese during submission prep** — marketing copy, not code, and it improves store search visibility in those countries.
- [x] **Data export — Journal PDF ✅ BUILT 2026-07-27 (gate green 403 tests; end-to-end verified on the sim — 19 KB PDF generated, iOS share sheet opened, document rendered and read).** Owner confirmed the scope below and it shipped as specified. `expo-print` added (works in Expo Go — verified). Pure `src/features/journal/export.ts` assembles the document and is where **both access rules are enforced**, not just described: **boundaries are filtered to `author_id === viewer`** (so a partner's temptation plan can never be exported by the other), and a **personal prayer is filtered to its author** as defence-in-depth behind the 00015 RLS. `exportHtml.ts` renders print-first HTML with **escaping as an explicit security boundary** — user-authored titles and bodies go into a document a WebView renders, which is the exact shape of an injection bug; a memory titled `<script>` prints as characters, and only `http(s)` image URLs are embedded. Boundaries land in a **separately labelled "Private appendix"**, never inside the chronological story, so they can't be mistaken for a memory by someone skimming. UI (`app/modal/journal-export.tsx`) is deliberately two toggles and a confirmation — its job is informed consent, not configuration; turning boundaries ON asks, turning them OFF never does (a confirmation on the safe direction just teaches people to tap through). **Reachable from Profile AND — more importantly — from the leave-couple screen itself**, directly above the irreversible button: that screen used to say "save it before you go" with nothing to tap, and after leaving, RLS makes it too late. 21 tests. **Still deferred:** the 30-day archive browser for the leaver (needs RLS rework across every couple-scoped table). Original design rationale follows. **(owner design round 2026-07-26; agent recommendation, owner to confirm).** Prompted by E14: someone leaving a five-year relationship losing every photo is the worst experience this app can produce, and the ToS already flags that an export path may be **legally required, not optional**, in some jurisdictions. **Recommended scope, in build order:** (1) **Journal → PDF** (NOT canvas/image: a PDF is portable, printable, searchable and readable in ten years without the app; photos embed cleanly; a chronological "Our Story" document is genuinely keepable). (2) **Answered prayers — yes**, they're testimony and the most-treasured record in faith apps of this kind; **active/unanswered prayers opt-in only**, being rawer and more private. (3) **Boundaries & temptation plans — EXCLUDED by default.** These are the most sensitive rows in the product: named struggles with lust, substances, anger, written for exactly one other person. A PDF is a file that gets emailed, backed up to a work laptop, and found. Victories could arguably qualify as testimony, but the whole category should default OFF behind a separate, deliberate confirmation. **Authorship rule:** shared content (journal, memories, dates) is exportable by EITHER partner — they both lived it; **boundaries are authored by one person and exportable only by their author.** Sequencing: journal PDF first (it covers the breakup case), answered prayers second, the rest only on user demand.
- [ ] **Comp / free-access list (NEW — owner, 2026-07-26): family & friends who use the app without paying.** Owner question — is it tied to Apple/Google IDs? **No**, it's tied to whatever identity we choose. **Recommended: a server-side allowlist keyed by lowercased email** (`comp_access` table + entitlement resolution ORs it with RevenueCat), so you can add someone BEFORE they sign up and it just works. **⚠️ Apple "Hide My Email" caveat:** a friend signing in with Apple may present a `@privaterelay.appleid.com` address, so email alone can miss them — the design needs a second path (grant by couple id after they sign up). **Alternative/complement: RevenueCat Promotional Entitlements** granted to the `couple_<id>` app-user id from the RC dashboard — no code, but only works after they've signed up and linked. Comping is App Store-legal (you're granting access, not selling outside the store). Must be enforced server-side, not client-side. **⚠️ The actual addresses must NOT be committed to this repo** — they're personal data belonging to real people, and the migrations folder is version-controlled. The owner holds the list (10 addresses as of 2026-07-26) and inserts them into the DB directly; the migration ships the TABLE, never the rows. **Owner: double-check `juniorbubu007@` / `juniorbubu009@` — they're one character off the `juniorbubub009@` on an existing test account, and a typo means someone silently gets no access with no signal why.** **Enforcement must live where every entry point sees it** — the 2026-07-26 onboarding-bypass bug proved a check at the root index alone is not a gate.
- [x] **Breakup / unlink handling (E14) — ✅ BUILT 2026-07-26 (gate green 339 tests). ⚠️ OWNER: apply `00035_leave_couple.sql`.** Owner approved the full agent design. Shipped: `leave_couple()` SECURITY DEFINER RPC + `couples.unlinked_at/unlinked_by`; `useLeaveCouple` (re-identifies RevenueCat to the INDIVIDUAL so the payer keeps their subscription, clears couple context, purges the persisted cache + upload outbox); `app/(tabs)/profile/leave-couple.tsx` confirm screen; a "Leave this couple" row under **Your relationship** (deliberately NOT in the Account danger zone beside Delete account) shown only when a partner is actually linked; 5 tests in `__tests__/api/leaveCouple.test.ts`. **Original design rationale (retained):** Today the ONLY exit from a couple is account deletion. **The F2 per-couple entitlement raised the stakes: a subscription is now attached to `couple_<id>`, so after a breakup BOTH people are stuck on an entitlement neither wants, and the payer can't take their own subscription with them.** That alone makes a minimal unlink a launch requirement. **Recommended design (agent, for owner approval):** (1) **Either partner can unlink UNILATERALLY and immediately** — never require mutual consent, because someone leaving an abusive relationship must be able to leave at once and without negotiation. (2) On unlink the couple space becomes **archived read-only for both** for a 30-day grace window in which either can **export** their memories and photos; after that it retires. (3) **Streak and points do NOT transfer** — they were earned together; a new relationship starts a new streak (also closes an obvious gaming vector). (4) **Neither partner can delete the other's authored content** — unlinking is not a weapon. (5) **Subscription follows the PAYER**: on unlink, re-identify RevenueCat from `couple_<id>` back to the individual user id so the person whose store account is billed keeps access; the other returns to the paywall. (6) Re-linking with someone new creates a fresh couple space. (7) The leaver's safety outranks a graceful notification — no "X left you" push. **Minimum viable for launch: "Leave this couple" in Profile → confirm → unlink → archive → RC re-identify.** Export and the archive browser can follow post-launch. ToS §8 + Privacy must update in the same pass.
- [x] **Devotional repeat policy — DECIDED (owner, 2026-07-10):** keep **communal rotation + grow the pool** (per-couple journeys rejected — they'd break the shared "today's devotional", the both-partners streak anchor, and the pegged widget content). The shown-once instinct is served instead by a **"you've read this before" affordance** (when rotation lands on a devotional the couple completed in a prior cycle, surface their previous reflection — `devotional_progress` already has the data). Affordance = v1.x build (first repeat is ~100 days post-launch after the 00024 batch). Pool grows ~30/month via `scripts/gen-devotionals.mjs`; **seasonal overrides (Advent/Christmas, Valentine's, Lent) go on the generation calendar** as `publish_date` rows — target the Advent batch by late October.
- [ ] **Widget v1 content pegged to today's devotional + streak flame (owner, 2026-07-05)** — rides the E7 WidgetKit spike (dev build required; iOS first per 2026-06-21 decision).
- [x] **Transparency & sharing round D1–D7 (owner locked ALL pre-launch, 2026-07-10; built same day, gate green 205 tests, sim-verified):**
>   - **D1 points-display bug FIXED:** the Us hub total (65) vs leaderboard (85) mismatch was a stale TanStack cache — nothing invalidated `points.total` and no realtime watches `points_ledger`; `logActivity` now invalidates all points keys. (Ledger itself was always consistent.)
>   - **D2 points history:** `app/modal/points-history.tsx` ("How you earned it" — This week/All time totals + per-row reason/who/when/+pts via `usePointsHistory` + pure `partitionPointsHistory`), linked from the Us hub points card. Couple-private; leaderboard exposes only totals (server-enforced).
>   - **D3 check-in sharing:** form now collects the 3 notes (they existed in DB but were never in the UI!) — gratitude ("something you appreciated") is ALWAYS revealed to the partner once both submit (shown in `CheckInComparison` via `getVisiblePartnerNotes`); growth area + prayer request are private with per-field **share toggles** (00025 columns). Privacy policy updated.
>   - **D4 audit trail:** migration **`00025_events_sharing_and_stamps.sql`** — `couple_events` table (couple-scoped RLS; feeds transparency + future year-end/anniversary recaps), `prayers.archived_at` (stamped on archive), `boundaries.deactivated_at/_by`, check-in share flags, streak trigger now logs `grace_used`/`streak_reset` events, boundaries added to realtime publication. App-side best-effort logger `src/api/events.ts`.
>   - **D5 boundaries history:** inactive rows no longer vanish — resolved temptation plans render as **"Victories 🏆"** (testimony framing = the locked never-resets victory record), retired boundaries as **"Past covenants"**, both with **"Bring back"** restore + attribution ("Retired by Tony · Jul 12" once 00025 stamps exist). **Resolve is author-only for temptation plans** (`canDeactivate` — a partner can't close someone else's struggle); boundaries retire-able by either partner. `useBoundariesRealtime` syncs partner changes live.
>   - **D6 tappable journal:** completed dates open **`app/modal/date-view.tsx`** (editable stars+note — edits also refresh the library aggregate; **"Save as a Moment"** creates a prefilled memory and lands on its detail where photos attach — date photos ride the existing pipeline); answered prayers open read-only **`app/modal/prayer-view.tsx`** (request + answered date + the cached AI-composed prayer — full circle).
>   - **D7 (owner-modified): NO weekly recap, ever.** Data gathering only (couple_events); recap cards limited to **year-end + app anniversary + relationship anniversaries**. `MILESTONE_PRESETS` chips on the special-day form (💜 dating / 💍 engagement / 💒 wedding anniversary, first date, first "I love you") put those anniversaries on the Journal timeline as first-class milestones.
>   - **AI-prayer disclosure sheet REMOVED (owner):** Compose tap silently records the consent stamp the edge function still requires; disclosure now lives in the Privacy Policy (attorney/store-review note added — revisit before submission).
>   - **⚠️ Owner: apply `00025` in Studio BEFORE testing check-in submit or boundary retire/resolve** — the app now writes the new columns and will error until it's applied. Regen types when convenient (`database.ts` hand-updated).
- [x] **How-it-works surfaces (E8) — BUILT 2026-07-18 (gate green 214 tests; sim-verified, logs clean).** Single copy source `src/features/how-it-works/content.ts` (mirrors 00012 streak rules + 00017 point values; `__tests__/features/howItWorks.test.ts` pins the values + conciseness). Surfaces: **`app/modal/how-it-works.tsx`** (streak rules / points table / honest rewards note) reachable from the Home streak card ("How streaks work" nested link), the Us hub ("How streaks & points work →"), and Profile settings row; **concise onboarding beats** (3-line strip on plan-summary, solo-days one-liner under the invite code); **partner-B team moment** `app/(onboarding)/team.tsx` — `usePartnerLink` now lands the joiner there with the partner's name (tested in `__tests__/onboarding/partnerLink.test.ts`) before the dashboard; **partner first names on Home** under the avatar pair ("AJ & Bexoni"). Website side shipped same day (`/how-it-works` + home FAQ + llms.txt/sitemap). Owner eyeball: link a fresh couple → team moment appears once; tap "How streaks work" on Home.
- [x] **AI rate limiting (E9) — ✅ BUILT 2026-07-18 (gate green 226 tests). ⚠️ OWNER: apply `00026_usage_rate_limits.sql` in Studio — enforcement is OFF until then (edge fns deliberately degrade open).** Migration: `usage_counters` table (couple/kind/day rows, couple-SELECT RLS, definer-only writes) + generic SECURITY DEFINER **`consume_usage_credit(kind)`** — limits live in the function's CASE so the owner can tune in Studio without an app release: `compose_prayer` **40/day + 150/month per couple**, `upload_presign` **400/day** (abuse guard). Day boundary = the couple's timezone (same rule as streaks) so "tomorrow" means their midnight. `compose-prayer`: input truncated to 1,000 chars; gate runs AFTER cache/crisis checks (free paths never charged); over-limit returns `{limited:'day'|'month'}`. `avatar-upload-url`: same gate, 429 on limit. Client: `getComposeLimitMessage` in `src/features/prayer/focus.ts` (owner register: "…maybe pray this one together in your own words" — warm, never error-styled; copy pinned by tests) rendered as a normal card in prayer-focus. ToS §5 fair-use + Privacy usage-counters notes added same pass. **Owner verify after applying 00026:** compose works normally; to see the cap without 40 taps, temporarily set the day limit to 1 in Studio (`CREATE OR REPLACE` the function), compose twice → gentle card, then restore 40.
- [x] **Streak breakdown v2 (E10) — ✅ BUILT 2026-07-25 (gate green 268 tests; Us hub sim-verified + 5 new interaction component tests). ⚠️ OWNER: apply `00030_longest_streak.sql` in Studio — until then the hub degrades gracefully (no best-streak line, no best/last labels, All filter falls back to a 30-day grid).** Migration: `couples.longest_streak` + `longest_streak_started_on/_ended_on` + `streak_started_on`, maintained by `update_couple_streak()` (record stamped whenever streak_count beats it) and **backfilled by replaying the exact 00012 rules over `devotional_progress` history** (couple-tz days, required members flips 1→2 at `linked_at`, 1 grace/Sunday-week); plus two SECURITY INVOKER RPCs: `get_activity_stats()` (all-time per-activity best streak + last-done — client only fetches 120 days) and `get_activity_daily_counts()` (one row per active day, feeds the heatmap "All" filter). Us hub v2 (`app/modal/us-hub.tsx`): hero relabeled **"day devotional streak"** + "Best streak: 12 days · Jun 3 – Jun 14" record line (kills the "0-day streak but full heatmap" contradiction); heatmap **7d/30d/All** chips (7d = one large tappable row w/ weekday letters; 30d/All = Sunday-aligned grid, All horizontally scrollable landing on recent weeks) + **tap-a-day tooltip** ("Jul 12 · 3 activities", cells carry a11y labels); per-activity rows now show "best 14 · last Jul 12" under the current streak. Pure math in `src/features/dashboard/activity.ts` (buildHeatmapCells/lastDaysCells/rangeDays/heatCellLabel/bestLastLabel/bestStreakLine — `buildHeatmapWeeks` retired); hooks `useActivityStats`/`useActivityDailyCounts` (All-counts fetched only when selected; `logActivity` now invalidates the whole activity key family). Privacy policy: derived-streak-stats clarifier added same pass. **Owner-locked: users can mute streak NOTIFICATIONS (G4) but never disable streaks; NO pause/vacation mode.**
- [x] **LOGO — ✅ IMPLEMENTED 2026-07-25 (app single icon + splash + Android adaptive + notification + favicon; landing OG/Twitter/favicon; brand purple unified to #9747FF everywhere; gate green 254 tests). Original spec (kept for reference):** vector masters live at **`/Users/Ebuka/Documents/VPS-agents/Bexhearts/SVG/`** (`Asset 12.svg` = cream mark on purple #9747FF circle · `Asset 13.svg` = purple mark on black rounded square · `Asset 14.svg` = purple mark on white rounded square · `Asset 15.svg` = deep-purple mark on white circle; mark = heart + winding-road + sparkle, palette #9747FF / #FFF8E7 / #2E0E5D). **Owner intent: Asset 12 artwork = light icon, Asset 13 artwork = dark icon.** Build notes (agent analysis, owner-agreed direction): (1) NEVER ship the baked circle/rounded-square shapes — iOS and Android apply their own masks; re-render each as a FULL-BLEED 1024×1024 square (mark centered at ~66–70% width). (2) Use iOS 18 icon variants via `app.config.ts` `ios.icon = { light, dark, tinted }`: light = cream mark on solid #9747FF; dark = TRANSPARENT background + mark (Apple supplies the dark backdrop — don't bake Asset 13's black square); tinted = grayscale mark on transparent. (3) Android adaptive icon: foreground = mark PNG w/ safe-zone padding, background = #9747FF; monochrome layer for Android 13 themed icons. (4) Splash: mark centered on cream; notification small icon (Android) = white-alpha mark 96×96. (5) Landing repo: swap the CSS `brand-mark`, favicon, OG/social images from Asset 12. (6) ⚠️ **Brand-color decision for the owner: logo purple #9747FF vs app token #9849FA (and cream #FFF8E7 vs bg #F8F4EC) — visually near-identical but should be unified** (recommend nudging `primary[500]` → #9747FF, or re-exporting the logo at #9849FA).
- [ ] **Alternate app icons — PREMIUM cosmetic (owner 2026-07-19; AWAITING the engineer's color variants; build AFTER the J1 dev build). GRAPHICS-PERSON DELIVERABLE SPEC (given to owner 2026-07-25 to reduce their workload — ONE file per variant, not 3 formats):** one **1024×1024 px PNG** (sRGB, flattened/opaque — iOS icons cannot be transparent) OR one full-bleed **SVG** per color variant; artwork must fill the whole square **edge-to-edge — NO built-in circle, rounded corners, padding, or transparent margin** (the OS masks it); keep key art within the center ~80% (safe zone); base palette brand purple **#9747FF** + cream **#FFF8E7**, each variant recolors the background/accent while keeping the heart-road-sparkle mark; name `icon-<colorname>.png`. The agent generates ALL downsized iOS/Android sizes + the Android adaptive foreground/monochrome layers from that one master — the designer does NOT supply multiple sizes or adaptive layers.** Locket-style icon picker — default icon free (store rules require a default), color variants **locked behind the premium entitlement** (lock badge → tapping a locked icon opens the paywall; reuse `useEntitlement`/PremiumGate). **Placement (agent recommendation, owner to confirm in build): Profile → Appearance section, an "App icon" grid directly under the System/Light/Dark selector** — same mental model (personalize the look), zero new nav. Tech: `expo-alternate-app-icons` config plugin (iOS `setAlternateIconName`, Android activity-alias); ALL variants must ship bundled in the binary (Apple rule — no downloadable icons), so each new variant = an app update + rebuild; **requires the dev build, not Expo Go**. ToS §monetization note ships in the same pass as the build (premium cosmetic).
- [ ] **Couple challenges** — themed multi-day plans (7-day prayer, 30 days gratitude); content-driven; premium + shareable. `date_ideas.is_challenge` already exists — decide reuse vs a dedicated `challenges` model.
- [ ] **Milestones & countdowns** — anniversary, engagement-versary, baptism/faith dates; surfaced on the dashboard + as notifications.
- [ ] **Home-screen widget** — verse of the day + streak + "partner just prayed". **Native iOS widget (WidgetKit) first; Android (Glance/App Widget) if feasible** — they're separate native implementations, not one shared widget. Needs a config plugin / native module (e.g. `@bacons/apple-targets` or a custom plugin) + a shared data channel (App Group on iOS) the JS can write to. **Bigger lift — spike Expo feasibility before committing (STEPS E7); may slip to v1.x, and Android may lag iOS.** (Owner-noted 2026-06-21.)
- [x] **Memories timeline ("Our Story" / Journal) — BUILT (E5+E6, 2026-07-04); PHOTOS + GALLERY BUILT (2026-07-04d).** Gallery: `avatar-upload-url` edge fn generalized to presign `avatars/<userId>` + `memories/<coupleId>/<memoryId>/<uuid>` (one bucket; memory membership proven by an RLS read); shared `src/api/uploads.ts`; `src/lib/imagePicker.ts` multi-select; `useAddMemoryImages` (upload → ordered `memory_images` rows); memory-form photo picking + upload on save; detail add-photos; fanned polaroids on feed cards + detail → **full-screen `PhotoViewer`** (paging, counter 1/N, share, iOS pinch-zoom, cream letterbox). Milestone **edit/delete** (special-day-form edit mode + Remove confirm) and **reaction/image realtime** (channel watches `memory_images` + `memory_reactions`, invalidates the open memory) also done. **⚠️ Owner: apply `00014_memory_images_realtime.sql`.** Remaining niceties: Android pinch-zoom, image delete/reorder, memory edit/delete. Original build (2026-07-04): `Journal` tab (`app/(tabs)/journal/`): reverse-chronological feed (`useTimeline` assembles memories + milestones + answered prayers + completed dates via the pure `buildTimeline`; completed devotionals excluded to avoid daily flooding), B+C hybrid `TimelineEntryCard` (purple-family glyph rail + tinted milestone cards w/ `getCountdown` + fuller memory cards), month section headers, `useJournalRealtime` live sync. Create flows: `journal-add` chooser → `memory-form` (title/description; photos = R2 fast-follow) + `special-day-form` (icon/title/date/color + countdown preview). Memory detail (`journal/[id]`) with **light reactions** (❤️/🙏 + one note per partner) + `FannedPolaroids` (renders when images exist). Hooks in `src/api/journal.ts`; tested (`__tests__/features/journal.test.ts`). **Remaining:** multi-image upload + full-screen swipeable viewer = the **gallery fast-follow** on the `bexhearts-memories` R2 bucket (see below); milestone edit/delete UI; reaction realtime. **DESIGN (still the spec):**
- [ ] ~~**Memories timeline ("Our Story" / Journal)** — a **reverse-chronological journal feed**~~ (design spec, now BUILT above) with **polymorphic entry cards** auto-weaving answered prayers + completed devotionals + completed dates + milestones + user-created posts (Couple Joy "Our Journal" pattern). **DESIGN LOCKED (owner, 2026-06-29):**
>   - **Layout = B+C hybrid:** a timeline *spine* with typed icons for light auto-entries + *scrapbook* hero cards for photo/date memories.
>   - **Identity = our locked purple/cream + square buttons** (the v0 maroon/serif exploration is retired — no rebrand). **Entry-type icons use ONE purple family** (differentiate by glyph, not by hue — no category rainbow) + a neutral for auto-entries.
>   - **Active-tab treatment:** filled icon on a light-purple pill (`#F1E7FE` behind `#9849FA`). **REFINEMENT DONE (2026-07-04):** the solid `#9849FA` filled glyph now *dominates* inside a snug chip (icon 26px, padding 12/4, radius 14 — light purple = surround/halo only), like the reference's dark-maroon icon inside soft pink. If the sim still shows a pale icon, it's a stale bundle → `npx expo start -c`.
>   - **Photo preview on a card = fanned polaroids** (cream frames, slight rotation, "N photos" chip) → tap opens a **full-screen swipeable viewer** (counter 1/N, pinch-zoom, share, close) with a shared-element spring open + cream letterbox + double-tap-to-❤️ a favorite.
>   - **Interaction = light reactions + one note** (❤️/🙏 tap + an optional one-line per partner) — NOT a full comment thread.
>   - **Home = its own `Journal` tab** (IA consolidation — see the nav note below).
>   - **Manual entry types (Couple Joy parity):** "Memory" (title + description + date + images) and "Special Day" (milestone/countdown: icon + title + date + optional time) — the latter merges with **E5 milestones**, so E5+E6 build together into the Journal.
>   - **Gallery sub-project:** an entry holds **multiple images** → a memory-images model (N ordered images/entry). **STORAGE DECISION (owner, 2026-07-04): ONE R2 bucket with key prefixes** — `avatars/<userId>` + `memories/<coupleId>/<memoryId>/<uuid>` (not separate buckets; simpler, one credential set). Owner has added the R2 env vars. Generalize the existing `avatar-upload-url` edge function to presign under the right prefix + multi-select picker. (Public URL = unguessable-but-public; switch to signed GET URLs later if true privacy is wanted.) Depends on the C3b R2 pipeline; photo upload is the fast-follow, text/milestone memories + reactions build now.
>   - completed dates remain the richest source (`couple_dates` rating + note + `photo_url`); completion is framed "capture this memory."
- [x] **Navigation round 2 — ✅ 2026-07-26 (owner testing feedback: "no back button or proper navigation… not a good and standard user experience"). Gate green 306 tests; every flow sim-verified.** (1) **Back buttons added to six screens that had none** — `dates/index`, `dates/[id]`, `connect/prayers`, `connect/boundaries`, `connect/check-in`, `devotional/[id]` (reusing the existing `BackButton`, which self-hides when there's nothing to go back to). Connect sub-screens already highlighted the Connect tab correctly, so the back button was the only gap there. (2) **Dates promoted from a hidden tab to a real pushed flow (owner picked this over a 6th tab):** `app/(tabs)/dates/` → `app/dates/`, registered on the ROOT stack, `Tabs.Screen name="dates" href:null` deleted; it now covers the tab bar like any iOS push, so there's no orphaned bar with nothing highlighted — the back button is the only affordance. Route paths updated in 5 places (QuickActions, `api/dates.ts` ×2 notification deep-links, `notifications/handlers.ts`, the list→detail push). **⚠️ A route-directory move needs a Metro restart (`npx expo start -c`) — a stale route map renders a bogus 6th tab.** (3) Home header: the couple caption is now two short lines capped at 132pt so it can't widen the header and shove the notification bell into the middle. (4) **Back-chevron alignment fixed** — `BackButton` assumes its parent applies the standard 16pt padding, but Prayers/Boundaries/Dates are edge-to-edge screens (each section pads itself), so the chevron hung off the screen edge while the title sat inset; it now takes an optional `style` and those three pass `paddingLeft: spacing.md`. (5) **Notification switches moved to their own page** (owner ask): `app/(tabs)/profile/notifications.tsx`, reached from a single "Notification settings" row matching the Restore Purchases pattern — Profile is a short list of rows again. Categories remain **ON by default** (a missing key = enabled, per `features/notifications/prefs.ts`), so new accounts get everything without visiting the screen.
- [x] **Navigation / IA restructure — DONE (2026-07-04):** tabs are now **Home · Grow · Connect · Journal · Profile** (`app/(tabs)/_layout.tsx`). Devotional tab relabeled **Grow** (leaf icon; route dir still `devotional`); **Journal** tab added; **Dates** hidden from the bar via `href: null` (still reachable from Home quick actions). Active tab = filled icon on a light-purple pill (`primary[100]` behind `primary[500]`). **Active-tab icon restyle DONE (2026-07-04)** — solid `#9849FA` filled glyph dominating inside a snug light-purple surround (see the Active-tab treatment refinement above). **Remaining:** fold date ideas/planning more explicitly into Home/Grow surfaces (currently a Home quick action); the **daily question (E1) lives in Connect** when built (NOT blended into boundaries).

## Phase 5 — Monetization
- [x] RevenueCat wrapper: init, identify, entitlement check, offerings, restore (`src/services/revenuecat/`)
- [x] Superwall wrapper: init, trigger paywall by event, identify/reset (`src/services/superwall/`)
- [x] `PremiumGate` component wraps gated screens (check-ins, boundaries) with paywall events
- [x] `useEntitlement` / `useOfferings` hooks
- [x] **Launch pricing LOCKED (owner, 2026-07-10; weekly added 2026-07-11): $79.99/yr (highlighted, "Save 78%" vs weekly, ≈$1.54/wk) + $12.99/mo (≈$3/wk) + $6.99/wk — ALL THREE at launch**, billed per couple, **3-day free trial on every plan**. *(The 07-10 "hold weekly for post-launch tests" call was reversed by the owner on 07-11 — the paywall now shows three ascending-commitment cards with per-week equivalents; sim-verified.)* Rationale: $79.99 sits at/above Hallow's proven faith price point, under Paired; weekly monetizes impulsive TikTok traffic. **Trial mechanism confirmed (owner ask, 2026-07-11):** `purchaseTrial` → RevenueCat `purchasePackage` → the store's native subscription sheet — the user commits to the subscription up front (payment method on their Apple/Google account required), $0 today via the free-trial intro offer, auto-billed at trial end unless cancelled. That is the payment-up-front/bill-after-window model, and the only mechanism the stores permit (apps never touch raw card details). `plans.ts` + `purchaseTrial` + tests updated; F2 creates the three store products (each with the 3-day free-trial intro offer).
- [x] **Comp / free-access allowlist — ✅ BUILT 2026-07-27 (gate green; 12 tests). ⚠️ OWNER: apply `00036_comp_access.sql`.** F2 locked everything behind the paywall, which is right for strangers and wrong for the people who must have access without paying — testers, pastors, family, and above all **the App Review demo account, without which a fully-paywalled app is rejected outright**. **⚠️ THE MIGRATION SHIPS THE TABLE, NOT THE ROWS** — the addresses are real people's personal data and this repo is version-controlled and will outlive the grants; the owner inserts rows by hand in Studio (copy-paste SQL is at the bottom of the migration). Design: keyed by **lowercased email** (normalized by trigger, because "a comp that silently doesn't apply" is worse than none) with a **grant-by-couple-id fallback for Apple "Hide My Email"** relay addresses the owner can't know in advance — which is also the conceptually right shape, since billing is already per couple. Revocable and expirable **without deleting the row**, so the record of who was comped and why survives. **RLS enabled with NO policies at all** (the `devotional_drafts` pattern): no client can read the list, so a comped person's email can never leak through the API; the only door is `has_comp_access()`, SECURITY DEFINER, which takes no arguments and answers **only about the caller** — there is deliberately no way to ask "is someone else comped?", which would make it an email oracle. Client: `checkCompAccess()` **fails CLOSED** on any error including the period before 00036 is applied. **Enforced through ONE hook, `useEntitlementAccess()` (RevenueCat OR comp), at EVERY enforcement point** — the tabs gate, `PremiumGate`, `SubscriptionStatus` and the paywall screen. That single-hook rule is not stylistic: the 2026-07-26 onboarding-bypass bug was exactly a check that lived at one entry point only, and a comp enforced at the tab gate but not in `PremiumGate` would produce the mirror-image bug (a comped pastor gets in, then hits "Unlock Premium" on a feature). Lives under the `entitlement` query root, which is already on `NON_PERSISTED_KEY_ROOTS`, so a revoked comp can't be restored from disk. Profile shows **"Complimentary"** with "nothing to pay and nothing to manage" rather than "Premium" — a comped user has no Apple subscription to go looking for. The onboarding paywall step now **auto-skips for anyone genuinely entitled** (the demo account's path), guarded so the dev-only `checkPremium → __DEV__` convenience does NOT skip it in Expo Go, where the paywall still needs eyeballing.
- [ ] Create products in App Store Connect / Play Console; configure RevenueCat dashboard (entitlement `premium`) + Superwall campaigns
- [ ] Real keys in env; end-to-end sandbox purchase test on a dev build
- [ ] Decide & enforce the free/premium feature split consistently (currently: check-ins + boundaries gated; devotional/date `is_premium` flags exist but are not enforced anywhere)
- [ ] **Shift the gating model toward content-volume gating** (decision 2026-06-13): candidate split — free = today's devotional + limited daily questions + basic widget; premium = full history, all challenges, unlimited questions, partner-comparison views, memories. Faith audiences convert better on "more of the thing" than on locked features. Reconcile with the check-ins+boundaries gating above and the Phase 4B features
- [ ] **Per-COUPLE billing** (DECISION 2026-06-17 — most important monetization wiring): the `premium` entitlement belongs to the COUPLE, not the individual. One partner trials/subscribes; the partner who links in INHERITS it and never sees a second paywall. **DECISION 2026-06-21:** tie the entitlement to a **stable couple-level RevenueCat alias** (NOT the individual `user.id` — today `AuthProvider` identifies RevenueCat by `user.id`, which must change here), so a leaving payer doesn't instantly strip the partner. **Policy: the partner keeps premium until the current billing period ends, then is prompted to subscribe** (no jarring mid-cycle downgrade). Reconcile with the dead `couples.subscription_tier` column (Phase 1).
- [ ] **Server-side RevenueCat customer deletion on account deletion** (GDPR/CCPA): the in-app delete clears Supabase, but RevenueCat still holds the customer; delete it via the RevenueCat REST API from the deletion processor / an edge function (secret key, server-side only). Pairs with B4's `process_due_account_deletions`.
- [ ] **Restore-purchases after re-signup**: a deleted+re-signed-up user gets a new `user.id` → new RevenueCat customer; surface "Restore Purchases" (see F3) so an active store subscription is recoverable.
- [~] **MONETIZATION FINALIZED (owner, 2026-07-04):** **hard paywall (trial → lockout), whole-feature gating, placed IN onboarding (after plan-summary, before partner-link), framed as a 3-day free trial.** The paywall shows the plans with a **"Subscribe now — no charge today"** CTA; picking a plan starts the **3-day free trial** (App Store/Play intro offer on the products, RevenueCat trial entitlement), they use the app free during onboarding + the trial, then auto-convert; cancelling/expiring = locked (hard). **Whole-feature split (starting point, tune in F1):** free-during-trial-only = everything; the *gated* features are check-ins, boundaries, daily question, dates, journal/memories (devotional + prayer + streak are the always-available core if we ever add a fallback tier — but current model is full lockout). Per-couple: the **inviter** starts the trial; the **partner inherits** + never sees the paywall. F1 = wire `PremiumGate`/`useEntitlement` consistently at the **couple** entitlement level (testable with a mocked entitlement); real products + trial config = F2/F3. **Store caveats:** disclose the trial terms + auto-renew price clearly on the paywall, offer Restore, link Terms/Privacy (App Store 3.1.2). *(Supersedes the earlier "content-volume gating" lean — owner chose whole-feature.)* **F1 BUILT (2026-07-04):** owned **Option-B "trial timeline" paywall** (`src/features/subscription/components/Paywall.tsx` — Today/Day 2/Day 3 timeline + annual/monthly plan cards + "No charge today · then <price> · cancel anytime" + "billed per couple" + Restore + Terms/Privacy) at `app/(onboarding)/paywall.tsx`, wired after plan-summary → partner-invite (replaced the Superwall no-op trigger). Purchase/restore → RevenueCat (`purchaseTrial`, `restorePurchases`); **entitlement gate defaults UNLOCKED in dev** (`checkPremium` returns `__DEV__` when RC unconfigured, `isRevenueCatConfigured()` gate) so local testing + `PremiumGate` features aren't blocked. Prices are placeholders (`plans.ts`). Tested `__tests__/features/subscriptionPlans.test.ts`. **Remaining (F2/F3):** real products + **3-day trial intro offer** in App Store Connect/Play + RevenueCat dashboard config + real keys; sandbox purchase E2E; Profile "Restore Purchases" entry; then flip on real enforcement.
- [~] ~~**Paywall model = free trial → hard paywall (NO freemium)** (DECISION 2026-06-17):~~ (finalized above) Superwall paywall fires in onboarding after the plan-summary, before partner-link, offering a 7-day free trial; trial unlocks everything; post-trial without subscribing = locked. Solo users hit ONE wall (this paywall), never a second partner-wall. **Trigger PLACED (C1b, 2026-06-21):** `triggerPaywall('onboarding_paywall')` fires from `PlanSummary` and no-ops in dev. **Remaining (Stage F):** the Superwall campaign + RevenueCat entitlement enforcement (per-couple, see above).
- [ ] Restore purchases surfaced in Profile/Settings UI
- [ ] **Install attribution + funnel analytics** — wire store/TikTok acquisition source into PostHog so marketing can see which content drives install → couple-linked → trial → paid (required by `docs/MARKETING.md` M0). Today PostHog tracks in-app events but not acquisition source.
- [ ] **PostHog ↔ expo-file-system incompatibility (must fix BEFORE enabling analytics)** — `posthog-react-native@3.16.1` eagerly calls expo-file-system's legacy `writeAsStringAsync`, **removed in SDK 54**, throwing at startup. Currently dodged by NOT constructing PostHog in dev/unconfigured (`src/services/analytics/client.ts`). When real keys are added (prod), this WILL crash unless we first upgrade `posthog-react-native` to an SDK-54-compatible version (or pass it custom AsyncStorage-backed storage).

## Phase 6 — Notifications & engagement (architecture owner-locked 2026-07-18 — full build spec in STEPS Stage G)
> **Stack (owner: "Supabase over Firebase"):** transport = **Expo Push Service** (tokens already registered; Expo wraps APNs/FCM — the only Google touchpoint is a one-time FCM server key upload to the Expo project for Android; **no Firebase SDK in the app**). Brain = **Supabase** edge functions + pg_cron + DB webhooks — all self-hostable on the Coolify VPS (enable `pg_cron`/`pg_net`; edge-runtime container ships with self-hosted; cron managed via SQL in Studio). **Privacy rule:** payloads never contain prayer/journal/check-in content (sensitive religious data) — generic titles only, content behind the tap; PRIVACY_POLICY.md updates per category in the same pass as each build.
- [x] Push token registration + permission flow + token saved to profile (`src/services/notifications/`)
- [x] Notification response listeners scaffold (`handlers.ts`)
- [x] **G1 — Push infra + in-app notification center — ✅ BUILT 2026-07-18 (gate green 232 tests; bell + inbox sim-verified, logs clean). ⚠️ OWNER: (1) apply `00027_notifications.sql`; (2) RESTART the functions server (`supabase functions serve --env-file supabase/functions/.env`) — a running serve does NOT discover the new `send-notification` folder (verified 404 until restarted); (3) regen types when convenient (database.ts hand-updated w/ notifications + usage_counters + notification_prefs so it compiles).** What shipped: `notifications` inbox table (recipient-only SELECT/UPDATE/DELETE, NO insert policy — service-role writes only via the edge fn; realtime publication) + `profiles.notification_prefs` (per-USER jsonb; absent key = enabled). **`send-notification` edge fn** — the ONE door for every notification (G2 events + G3 cron will call it): user-JWT callers may only notify their own partner; service-role callers (cron) anyone; muted category → nothing; **quiet hours (couple timezone) suppress the PUSH but still write the inbox row**; Expo Push API send w/ `DeviceNotRegistered` → token pruned; inbox row always first (source of truth — the whole center works in Expo Go with zero push infra). Prefs logic canonical + tested in `src/features/notifications/prefs.ts` (mirrored in the fn, streak-engine pattern). **Bell on Home** (unread badge, realtime via `useNotificationsRealtime` in the tabs layout) → **`app/modal/notifications.tsx`** (unread highlight, tap = mark-read + deep-link via stored `route`, mark-all-read, warm empty state). **Push-token hygiene:** cleared on sign-out AND at deletion-request (profile survives the 7-day grace, so explicit clear needed). `handlers.ts` push-tap now routes by `data.route` (legacy type-switch kept). **Owner live test after 00027 + serve restart:** in Studio run `INSERT INTO notifications (recipient_id, couple_id, category, title, body, route) SELECT id, couple_id, 'partner_activity', 'Bexoni is praying for you 💜', null, '/modal/us-hub' FROM profiles WHERE email = '<your test email>';` → badge appears live on that sim, tap opens the Us hub. OS-level push delivery itself = dev-build verify at J1 (sims/Expo Go have no push token — by design the inbox still works).
- [x] **G2 — Event-driven (partner-activity) — ✅ BUILT 2026-07-19 (gate green 239 tests; END-TO-END VERIFIED on the sims: edge fn → inbox row → realtime → bell badge "1" appeared live with the app untouched). ⚠️ OWNER: apply `00028_dates_suggest_and_push_debounce.sql` (dates-v2 columns + `notifications.pushed_at`; suggest/accept errors gracefully until applied).** Wired (all via `notifyPartner()` in `src/api/notifications.ts`, fire-and-forget after the action succeeds; copy = names + generic wording, NEVER content): shared prayer added · prayer answered · **journal moment added** · memory reaction · check-in "your turn" · partner-finished-devotional ("keep the flame together") · partner linked ("invite accepted") · **E3 praying-for-you** = (a) one-tap "🙏 Tell {partner} you're praying" on the Prayers screen + (b) finishing a prayer-focus session auto-notifies "{name} just prayed for your requests". **Debounce (M2):** ≤1 PUSH per category per 2h (partner_activity) via `notifications.pushed_at` — inbox rows always written; logic canonical+tested in prefs.ts, mirrored in the edge fn. **Dates v2 (M3):** `suggested_by`/`accepted_at` on couple_dates; "Suggest to {partner} 💌" on the idea detail; Our Dates gets a **Suggested** section — receiving partner sees Accept 💜 / Pass, suggester sees "Waiting…" (`canRespondToSuggestion`, tested); accept notifies the suggester 🎉, pass removes the row; legacy rows untouched (dateHelpers tests). **BUGFIX found during verify: the realtime subscription moved from the tabs layout to the ROOT layout** — entered-via-modal contexts (deep links, future push taps) never mounted tabs, so the bell missed live rows. Default-OFF stays: ratings, boundary edits.
- [x] **G3 — Scheduled (pg_cron) — ✅ BUILT 2026-07-19 (gate green 250 tests). ⚠️ OWNER: EDIT then apply `00029_notification_cron.sql`** (two placeholders — the functions URL + service key; instructions in the file) **+ RESTART the functions serve** (new `notification-sweep` folder). Architecture: pg_cron (+pg_net) POSTs hourly at :05 to the **`notification-sweep` edge fn**, which computes each couple's LOCAL hour/date/dow (couples.timezone) and runs the **sweep planner** (canonical + 8 tests in `src/features/notifications/sweep.ts`, mirrored in the fn): **9am** devotional reminder (only members not done) + "grace covered you" notice (from 00025's grace_used events) · **10am** milestone countdowns (3-days-out + day-of, both members) · **12pm** the ONE daily prayer digest (count of active shared requests; locked 2026-06-29) · **Sun 6pm** weekly check-in reminder (members not yet in) · **7pm streak-at-risk** (streak ≥ 3, members missing today — "🔥 Your N-day streak is on the line"). Every action dispatches through `send-notification` (one door: prefs/quiet-hours/debounce/token-pruning apply) and dedupes vs the last 20h of notifications (idempotent — manual re-runs send nothing). **Also finally schedules `process_due_account_deletions` (daily 03:00 UTC — owed since B4; pure SQL, no placeholders).** Verify + hand-trigger commands in `docs/EDGE_FUNCTIONS.md`. Prayer staleness-triage nudge ("Still praying for this?") deferred to a later pass — it needs per-prayer age UX, not just a ping.
- [x] **G4 — Notification preferences UI — ✅ BUILT 2026-07-19 (gate green 254 tests; sim-verified, logs clean). THE NOTIFICATION EPIC (G1→G4) IS COMPLETE.** Profile → Settings gains a **Notifications** card: four purple switches (Partner activity / Daily reminders / Streak alerts / Milestones, each with a plain-English hint) + **Quiet hours** (v1 = one fixed 10pm–8am window behind a switch; custom times later; copy says pushes still land in the inbox). Per-USER, written to `profiles.notification_prefs` via immutable helpers (`setCategoryPref`/`setQuietHoursEnabled` in prefs.ts, tested incl. "toggled-off category is honored by the enforcement gate") — the G1 edge fn has enforced this column since day one, so the switches worked the moment they rendered. Owner-locked rules hold: streak NOTIFICATIONS mutable, streaks never disableable, no pause mode. Owner eyeball: flip a switch → partner action of that category stops arriving; quiet-hours ON at night → inbox row only.
- [ ] **Manual/campaign broadcasts — DESIGNED, DEFERRED (owner 2026-07-18: not until real users exist):** a `broadcasts` table the owner inserts into via Studio; a cron sweep sends pending rows through `send-notification`; no admin UI. Do not build yet.

## Phase 7 — Quality & testing
- [x] Jest infra: native-module mocks (`__tests__/setup/jest.setup.ts`), `renderWithProviders` helper
- [x] Unit tests: auth store, couple store, invite-code utils
- [~] Tests for `getWeekOf` / date helpers (streak & check-in correctness depends on them) — **streak transition + grace + day-state covered** in `__tests__/features/streak.test.ts` (D6, 2026-06-29); direct `getWeekOf`/`getDeviceTimeZone` unit tests still a nice-to-add.
- [ ] API hook tests with mocked Supabase client (the upsert bugs below would have been caught)
- [ ] Component tests for the 4 main forms (sign-up, profile setup, check-in, prayer)
- [x] **Reliability round (H2) — ✅ BUILT 2026-07-25 (gate green 293 tests; boot/journal/cold-restart sim-verified, logs clean). No migration.** (1) **Image compression** — `src/lib/imageCompression.ts` (`resizeSpec` shrinks the longer side to ≤1600px, never upscales; re-encode JPEG q0.8; best-effort fallback to the original) applied to avatar uploads and every memory photo at outbox-enqueue. (2) **Upload outbox** — pure math in `src/features/uploads/outbox.ts` (backoff 0s→30s→2m→10m→30m, 5 attempts then parked, oldest-first, revive-on-restart), AsyncStorage-persisted queue in `src/stores/uploads.store.ts` (cached-promise `ensureHydrated` so enqueue can't clobber a prior session's jobs), worker `src/services/uploads/worker.ts` (single-flight drain: presign → `expo-file-system/legacy` `uploadAsync` with the **iOS BACKGROUND session** → insert the `memory_images` row per photo → invalidate; wake-timer for backoff), `useUploadOutbox` in the root layout resumes on start/reconnect/foreground; memory save is now instant with "N of M photos still uploading — we'll finish in the background" pending labels on the memory detail + timeline cards (`usePendingUploads`); `buildMemoryImageRows` retired. (3) **Query-cache persistence** — `PersistQueryClientProvider` + AsyncStorage persister (24h maxAge, gcTime raised 30min→24h, app-version buster): cold start offline renders last-known-good devotional/prayers/journal/streaks (privacy: device-local cache noted in PRIVACY_POLICY). (4) **Offline banner + consistent mutation errors** — global `OfflineBanner` above the navigator (NetInfo) and a `MutationCache.onError` that toasts `friendlyMutationError` (warm copy, raw driver text never surfaces) for any mutation without its own onError (`meta.suppressGlobalError` opts out). **⚠️ MMKV gotcha found + fixed: `new MMKV()` THROWS in Expo Go and `src/lib/storage.ts` ran it at import time (was dead code until H2) — now guarded with an in-memory fallback; outbox + query cache deliberately use AsyncStorage so persistence works in Expo Go.** **⚠️⚠️ CRASH SHIPPED BY THIS ROUND, FOUND IN OWNER TESTING 2026-07-25 + FIXED:** the persisted cache is **JSON**, and `useDateIdeaAggregates` returned a **`Map`** — `JSON.stringify(new Map())` is `{}`, so on the next cold start `aggregates?.get(...)` was undefined and the date Ideas list threw `aggregates?.get is not a function`, which the ROOT ErrorBoundary caught and replaced the whole app with (that is also why the LDR toggle appeared dead — the entire tree was the error screen). **Fix, three layers:** (1) aggregates now returns a plain `Record<string, IdeaAggregate>` (call site `aggregates?.[item.id]`) — also backward-compatible with already-poisoned caches, since the stale `{}` now reads as "no aggregate" instead of crashing; (2) **`src/features/cache/persistPolicy.ts`** gates the persister via `dehydrateOptions.shouldDehydrateQuery` — refuses any value that isn't JSON-round-trippable (Map/Set/Date/class instance, nested included) AND keeps `entitlement`/`offerings` off disk entirely (a day-old `premium: true` restored from disk would unlock paid features after a lapse; stale offerings would show wrong prices); (3) `__tests__/features/persistPolicy.test.ts` pins all of it, including the exact Map shape that shipped. **THE RULE FOR FUTURE WORK: everything stored in a query must survive `JSON.parse(JSON.stringify(x))` — the whole `src/api` layer was audited and aggregates was the only offender.** **⚠️ SIGN-OUT HYGIENE REGRESSION CAUGHT + FIXED IN THE SAME PASS:** persisting the query cache meant `queryClient.clear()` (memory only) would have let the NEXT user on a shared device restore the previous user's cached prayers/journal from disk on cold start — the AuthProvider `SIGNED_OUT` handler now also calls `purgePersistedQueryCache()` + `clearOutbox()` (keys centralized in `src/lib/persistedStorage.ts`; same rule as G1's push-token clearing; covers account deletion too, which routes through the same handler). Tested in `__tests__/features/signOutHygiene.test.ts`.
- [x] **Analytics instrumentation — ✅ BUILT 2026-07-27 (gate green 403 tests; events VERIFIED landing in PostHog from the real SDK path on the sim).** **Correction to the note below: `posthog-react-native@3.16.1` and all four peer packages were ALREADY installed** — the blocker was never installation, it was that `src/services/analytics/client.ts` deliberately refused to construct the client outside production to dodge the SDK-54 crash, so analytics could not be verified before shipping. **THE SDK-54 CRASH IS FIXED PROPERLY:** posthog's default storage picker (`buildOptimisiticAsyncStorage`) prefers expo-file-system and calls the legacy `documentDirectory`/`writeAsStringAsync` API removed in SDK 54; passing `customStorage` backed by AsyncStorage short-circuits that picker entirely. (The other legacy-FS path, `getLegacyValues()`, is fully try/caught inside the SDK — verified by reading the compiled source.) Session replay is **off permanently** — video of a prayer journal is a non-starter. **Every event carries `app_env`** (development/production) so dev traffic filters out of real funnels with one property filter, which is what makes verifying on a simulator possible at all. **THE HARD CONSTRAINT IS ENFORCED MECHANICALLY, not by convention:** `src/features/analytics/schema.ts` gates every event through a **key allowlist** (a property key not on the list is DROPPED, so `note`/`title`/`body`/`name` have no vehicle) plus **value rules** (enums where a closed set exists, otherwise short slugs — a sentence cannot pass, because whitespace is rejected). 9 tests pin it, including one that fails if a content-shaped key is ever added to the allowlist. **`identify()` sends the user UUID and NOTHING else — the email trait was removed**, resolving the "[MINIMIZE AT LAUNCH: review whether email is necessary]" flag that was sitting in PRIVACY_POLICY.md. **Funnel wired:** `app_opened` (cold start + every foreground return, carrying `days_since_first_open`/`is_first_open`) · `onboarding_step_completed` (one event, `step` + `step_index` + `step_count`, so a single event powers a step-by-step drop-off chart) · `paywall_shown` (fires on RENDER not on tap — the drop-off that matters is people who saw the price and left — with `placement: onboarding | gate`, so a lapse is distinguishable from a first look) · `trial_started` + `subscription_started` · `partner_linked` (**BOTH sides**, `role: inviter | joiner`; the inviter observes it as a state change so it rides a once-per-install `trackOnce` guard — without the inviter's side the funnel dead-ends at `trial_started` for exactly the person who paid) · `devotional_completed` · `d1_returned` / `d7_returned` (pure day math in `src/features/analytics/retention.ts`, device-local calendar days, clamped against a backwards clock, JSON-round-trippable per the H2 rule). Per-install analytics state is purged on sign-out alongside the query cache. **Dev-only `client.debug(true)`** prints every capture/flush to the device log — that is how this was verified and how it gets re-verified later. Original note follows. **(NEW — owner, 2026-07-26; HIGHEST-VALUE non-feature work, agent assessment).** **PostHog project EXISTS; key + host are in `.env` (US Cloud, project 529360 — the project ID is NOT needed by the app, only the `phc_` key and host).** **The React Native SDK is NOT installed** — `npx expo install posthog-react-native expo-file-system expo-application expo-device` — so `track()` in `src/services/analytics/events.ts` is still a stub and every event currently goes nowhere. **Why it matters most:** the owner locked a HARD PAYWALL with a card required up front (F2), and there is no way today to see install → paywall-shown → trial-started → subscribed → D7/D30 retained. When conversion disappoints, the funnel says *where*: a cliff at trial-start means price/copy, at day 3 means the trial didn't demonstrate value, at D30 means retention not monetization. **Funnel to instrument:** app_opened · onboarding_step_completed · paywall_shown · trial_started · subscription_started · partner_linked · devotional_completed · D1/D7 return. **HARD CONSTRAINT: events are STRUCTURAL ONLY** — never prayer text, journal content, check-in notes, or names. Religious practice is sensitive data; the Privacy Policy already names PostHog, keep it true.
- [x] **Sentry crash reporting (H2·M5) — ✅ BUILT 2026-07-25:** `src/services/sentry.ts` — lazy `require('@sentry/react-native')` ONLY when `EXPO_PUBLIC_SENTRY_DSN` is a real https DSN AND not Expo Go (`sentryEnabled`, tested); `sendDefaultPii: false`, traces 0.2; `captureException` wired into `ErrorBoundary`; `.env.example` documents the placeholder-off behavior. Source-map upload via the `@sentry/react-native/expo` config plugin deliberately deferred to the J1 dev-build pass (owner creates the Sentry project + DSN then too). Privacy policy crash-reporting disclosure added same pass.

## Phase 8 — UI/UX polish (only after Phases 3–5 verified working)
- [x] Design tokens (`src/theme/`), shared primitives (Button, Card, Input, Avatar, Badge, EmptyState, ErrorBoundary…)
- [ ] Decision: stick with the hand-rolled component kit vs adopt a UI library — **recommendation: stick with what exists**, it's consistent and complete enough; revisit only if a specific need appears
- [ ] Loading skeletons, pull-to-refresh, haptics pass, animations (reanimated is installed, unused)
- [x] **Dark mode + brand color scheme — ✅ BUILT 2026-07-18 (gate green 224 tests; sim-verified all three modes, device logs clean).** How it works: `src/theme/colors.ts` now holds complete `lightColors` + `darkColors` token sets (same shape; the 50→900 scales INVERT lightness in dark so existing pairings like "primary[100] chip + primary[700] text" stay readable; `#9849FA` anchored at 500 in both; `text.inverse` stays near-white in both because every call site is text-on-brand-surface). The legacy `colors` export is **LIVE** (top-level getters resolve the active mode at access time) so inline JSX usage needed zero changes; module-scope `StyleSheet.create` blocks were codemodded (54 files) to `themedStyles(() => (...))` (`src/theme/themedStyles.ts` — per-mode lazy cache behind a Proxy; style bodies byte-identical). **Mode resolution = the owner's rule: Settings → Appearance decides — explicit Light, explicit Dark, or System (follows the OS)** via `resolveThemeMode` in `src/theme/useTheme.ts` (unit-tested matrix in `__tests__/theme/theme.test.ts`); `useThemeMode()` runs in the root layout's `ThemedApp`, which re-mounts the UI subtree (`key={mode}`) and drives the StatusBar; providers/auth/query state sit above the key and are untouched by a toggle. `app.config.ts` → `userInterfaceStyle: 'automatic'` (**⚠️ requires a Metro restart to pick up — stale manifests keep the app light; that cost 20 min of debugging**). Module-scope constant traps fixed: Button/Badge/Card variant maps → render-time functions; milestone SWATCHES deliberately snapshot the LIGHT palette (stored to the DB as concrete hex). Badge success/warning pastels now come from theme tints. Sim-verified: system+dark-OS→dark, explicit-light+dark-OS→light, explicit-dark+light-OS→dark; Home/Us-hub/How-it-works/Profile/Prayers/Journal eyeballed dark. **Known polish left:** ~~toast banners still light-styled~~ **RESOLVED 2026-07-19 — custom toast host (`src/components/ui/AppToast.tsx`): theme-aware AND every popup now carries a dismiss ✕ (owner ask)**; a both-modes sweep of the long-tail screens rides normal usage. Original spec follows for reference: `src/theme/colors.ts` has no light/dark split, and `app.config.ts` forces `userInterfaceStyle: 'light'`. **UPDATE (2026-07-04f): the Settings → Appearance selector (System/Light/Dark) is BUILT** and persisted (`ui.store.appearance` + AsyncStorage `bexhearts.appearance`) — picking Dark shows an honest "arrives in an upcoming update" toast; when this phase builds the theme resolver it must read that stored preference. Brand palette **locked 2026-06-18, sourced from bexoni.com** (oklch → hex):
>   - **Primary purple `#9849FA`** — already applied to `colors.primary` (violet scale anchored at `500`), `text.link`, and the notification color in `app.config.ts`. Stays the same in both modes.
>   - **Light mode:** bg cream `#F8F4EC` (applied), surface/card `#FEFCF7` (applied), elevated `#FFFFFF`, text near-black `#141517`, border `#DAD7D0`.
>   - **Dark mode (to build):** bg black `#13161A`, card `#171B20`, accent surface `#1F2329`, text near-white `#FAFAFA`, muted text `#8B9098`, border `#2A2E35`.
>   - **Agent TODO when implementing:** (1) restructure `colors.ts` into shared scales (`primary`/`secondary`/`accent`/`neutral`) + separate `light`/`dark` token sets for `background`/`surface`/`text`/`border`/`overlay`; (2) add a theme resolver (extend `src/stores/ui.store.ts`) honoring system scheme + a persisted manual override (`expo-secure-store`/AsyncStorage in Expo Go, MMKV after the dev build); (3) expose a `useTheme()` hook and replace direct `colors.background`/`colors.text.*` reads in components/primitives with active-theme tokens; (4) remove the forced `userInterfaceStyle: 'light'` and drive the root `StatusBar` style dynamically; (5) verify every screen + all shared primitives (Button/Card/Input/Avatar/Badge/EmptyState/ErrorBoundary) in both modes.
- [ ] Accessibility pass (labels, touch targets, dynamic type)
- [ ] Real app icon + splash (current assets are defaults)

## Phase 9 — Production backend (VPS / Coolify)
- [ ] Deploy Supabase via Coolify; HTTPS domain; strong secrets
- [ ] Apply migrations + production seed content via VPS Studio (manual, owner-run)
- [ ] SMTP for auth emails; decide on email confirmation + deep links
- [ ] Backups (automated pg_dump or Coolify backup schedule) — non-negotiable before real users
- [ ] Production env vars in EAS (URL + anon key per environment)
- [ ] Staging vs production strategy (even just two Coolify Supabase instances)

## Phase 10 — Release
- [ ] `eas init` → real projectId in `app.config.ts`
- [ ] Dev builds both platforms; verify native features (push, purchases, Apple Sign-In)
- [ ] Real devotional/date-idea content (seed data is 7 days/10 ideas of sample copy) — built as stage-aware packs per the positioning: dating, engaged/premarital, newlywed
- [~] **Landing-site round 2 (2026-07-18):** `/invite/[code]` + `/invite` (invite links no longer 404; noindex; store buttons appear when `appLinks` in `lib/site.ts` is filled), `/how-it-works` (streak/points/rewards breakdown + "you're a team"; sitemap + llms.txt + home FAQ/link updated), **waitlist locked to service-role** — anon fallback removed from the API route AND **⚠️ owner runs `supabase/waitlist_rls_fix.sql` (in the LANDING repo) against the waitlist Supabase project** to drop the anon insert policy that let anyone bypass the rate limiter. `next build` green.
- [~] Privacy policy + Terms hosted at real URLs — **SITE READY (2026-07-12):** the landing repo (`/Users/Ebuka/Projects/bexheartslandingcodex`, Next.js) serves full `/privacy` + `/terms` pages ported from the docs drafts at **bexhearts.com** (owner-purchased domain); `src/constants/app.ts` now points at them (support email → support@bexhearts.com — ⚠️ owner creates the mailbox). **Remaining: owner deploys the site + DNS.** Keep site legal pages in sync with `docs/PRIVACY_POLICY.md`/`TERMS_OF_SERVICE.md` in the same pass as future changes. **Required by:** Apple App Store Connect (Privacy Policy URL is mandatory), Google Play (Data Safety) + the Google OAuth consent screen *when published to Production* (optional while in Testing).
- [ ] **Data-collection inventory** (for Apple "App Privacy" nutrition label + Google Play "Data Safety" form). Collected, **linked to the user's account, NOT used for cross-app tracking**: Contact info (email), Name, Photos (avatar, optional), User content (prayers, devotional reflections, check-in notes, boundaries, dates), **Sensitive info — religious beliefs** (denomination + faith content — must be disclosed on both forms), Identifiers (user id, Expo push token), Purchases (subscription status via RevenueCat), Usage/Analytics (PostHog, when enabled), Diagnostics (crash reporting, planned). Google OAuth itself only requests **email + profile + openid** (non-sensitive → no Google verification needed).
- [ ] App Store assets, screenshots, review-readiness (incl. account deletion from Phase 2)
- [ ] TestFlight + Play internal testing round with real couples
- [ ] Submit

---

## 🐛 Known bugs (verified by code reading, not yet fixed)

1. ~~**Weekly check-in resubmission fails — `src/api/check-ins.ts` `useSubmitCheckIn`**~~ — **✅ RESOLVED 2026-06-29 (D3):** the upsert now passes `onConflict: 'couple_id,user_id,week_of'` (D3·M2) AND migration `00008_check_ins_update_policy.sql` adds the missing UPDATE RLS policy (D3·M1), so resubmitting the same week updates instead of being denied. Tested in `__tests__/api/check-ins.test.ts`. **⚠️ Requires owner to apply `00008` in Studio.**
2. ~~**Devotional re-completion fails — `src/api/devotionals.ts` `useCompleteDevotional`**~~ — **✅ RESOLVED 2026-06-29 (D1·M1):** the upsert now passes `onConflict: 'devotional_id,user_id'` to match `UNIQUE(devotional_id, user_id)`, so re-completing updates instead of throwing. (UPDATE policy already existed → code-only fix.) Tested in `__tests__/api/devotionals.test.ts`.
3. ~~**Password reset link is a dead end**~~ — **✅ RESOLVED 2026-06-21 (B2):** replaced with the 6-digit OTP code flow (`app/(auth)/reset-password.tsx`, `verifyRecoveryOtp` → `updatePassword`); the `bexhearts://reset-password` deep link is gone.
4. ~~**Streak is display-only**~~ — **✅ RESOLVED 2026-06-29 (D6):** migration `00012_streak_engine.sql` adds the `update_couple_streak()` trigger (devotional-completion anchor, both-partners-required, weekly grace day, couple-timezone day boundary); the dashboard now reads the live count + a "waiting on partner" nudge. Logic mirrored/tested in `src/features/dashboard/streak.ts`. **⚠️ Requires owner to apply `00012` in Studio.**
5. ~~**Realtime channel crash on remount**~~ — **✅ RESOLVED 2026-07-11:** red screen "cannot add `postgres_changes` callbacks … after `subscribe()`" (owner hit it adding a photo / around delete-account). Cause: supabase-js returns the EXISTING channel instance for an already-registered topic, so any fast remount (sign-out/in, error-boundary recovery, fast refresh) re-ran `.on()` on a subscribed channel. Fix: `freshChannel()` in `src/services/supabase/realtime.ts` removes any prior channel for the topic before creating, and every realtime hook now tears down with `supabase.removeChannel()` (full deregistration) instead of `unsubscribe()`. Tests updated.
6. ~~**Every toast in the app was silent**~~ — **✅ RESOLVED 2026-07-11:** `ui.store.showToast` only set state that NOTHING rendered — the actual toast UI is `react-native-toast-message`'s host in `app/_layout.tsx`. The store now bridges to `Toast.show()`, so "Note saved ❤️", the answered-prayer celebration, copy confirmations etc. all actually appear. Also: memory-note send now dismisses the keyboard + success haptic + error toast; photo upload shows "Photo added 📷" / a clear error toast.

## 💡 Backlog / ideas (unprioritized — NOT in v1.0)
> Note: memories timeline, milestones/anniversary reminders, and the home-screen widget were promoted OUT of here into the v1.0 engagement layer (Phase 4B).
- **"Faith garden / tree" streak visual** — the one nurture-mechanic kept from Cozy; a v1.x lightweight streak reward, NOT a full pet/economy
- **Quizzes / relationship games** as an acquisition/virality play (deferred from the engagement layer)
- **Secular sibling app** — same codebase, different content pack; only after this app reaches PMF
- Devotional content CMS or admin script instead of raw SQL seeds
- Localization (es/pt strong fit for the niche)

---

*Last full audit: 2026-06-10 (schema, all API modules, services, providers, stores, screens, tests read end-to-end).*
*Updated 2026-06-13: positioning locked (faith-forward; dating→engaged→newlywed beachhead); engagement layer (Phase 4B) + content-volume gating added from the Couple Joy + Cozy competitive analysis; "tests ship with the work" protocol rule added. Docs reorganized under `docs/` with `STEPS.md` + `MARKETING.md` indexed in `CLAUDE.md`/`AGENTS.md`.*
